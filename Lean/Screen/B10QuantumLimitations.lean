import EventAlgebra.Robertson
import EventAlgebra.Superselection
import ExteriorComponentBridge
import QuantumMatterIntegration

/-!
# B10 finite quantum limitation package

This root collects the supplied-state Robertson theorem, the exact
partition-operational quotient, the five-mode exterior component bridge, and
the typed component/sector/Z6 integration.  The individual modules state the
required inputs and physical boundaries.  Importing them together adds no
source-selection or laboratory premise.
-/

