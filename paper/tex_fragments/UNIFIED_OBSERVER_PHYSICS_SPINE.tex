\section*{Unified observer-to-physics spine}

The three core papers use one typed construction. At cutoff \(r\), a bounded
observer-patch federation supplies local accessible algebras, overlap ports,
public records, feedback or repair moves, and checkable evidence. Quotienting
presentation redundancy and applying the accepted repair law gives

\[
\mathsf{PatchFederation}_r
\longrightarrow
\mathsf{PhysicalQuotient}_r
\longrightarrow
\mathsf{PublicNormalForm}_r.
\]

This finite consensus arrow is the common substrate. It does not produce an
equilibrium state, a modular automorphism, event geometry, or a gauge current.
Those objects enter through separately typed, source-derived maps on one
refinement tower:

\[
\begin{array}{c}
\mathsf{PublicNormalForm}_r
\longrightarrow
(\rho_r,K_r^{\rm mod},\mathsf{Geom}_r)
\longrightarrow
\mathsf{BW/Null/Event}_r
\longrightarrow
\mathsf{Einstein}_r,\\[4pt]
\mathsf{PublicNormalForm}_r
\longrightarrow
(\mathsf{A5Carrier}_r,J_r)
\longrightarrow
\mathsf{CompactCurrent}_r
\longrightarrow
\mathsf{SM}_{Q0,r}
\longrightarrow
\mathsf{QFT}_{Q1\text{--}Q4,r}.
\end{array}
\]

The first line requires an independently produced noncommutative state tower,
modular transport, geometric flow, event charts, and stress response. Here
\(H^3\) is the future-timelike frame fiber over the event base. The second line
requires a full-rank compact skew-adjoint, commutator-closed physical current
lift carrying the same inner \(A_5\) action. Register adjacency, repair rates,
and modular grading do not supply that current. Standard Model recognition at
Q0 does not construct the Q1 action, either Q2 quantum landing, the Q3
renormalized pole engine, or the Q4 continuum theory. The strict one-loop
\(W/Z\) map is a conditional Q3 algebra theorem until its source, action,
matching, FJ, BRST/Nielsen, covariance, uncertainty, and clock receipts exist.

Both lines must descend from one source-bound, refinement-natural tower before
they may be composed. A common manifest records source hashes, quotient maps,
scale maps, and tails; it cannot turn one ledger into another. This is the
unified claim: consensus, geometry/gravity, and gauge/QFT are composable typed
branches of one observer-like self-reading system. It is not a claim that a
finite run has produced every branch premise.

Two exact controls fix the present empirical boundary. The finite \(A_5\)
software packet has \(60\) reachable correctable public records on
\(\mathcal H_k=\ell^2(A_5)\otimes\mathbb C^k\), so

\[
M_0=60,\qquad D_{\rm raw}=60k,\qquad
\Delta_{\rm raw}=60(k-1).
\]

Raw equality occurs only at \(k=1\). The publicly inert multiplicity makes
\(D_{\rm raw}\) implementation-dependent, so this is an evaluator control and
raw-carrier minimum rather than physical \(N\)-closure. Separately, the
repaired \(4{,}096\)-patch \(H^3\)/KMS campaign cell has a valid frozen
instrument and exact disk replay. Its scientific status is incomplete and
physical promotion is forbidden: the support tower and \(M_4\) state were not
produced independently by the source, the modular and geometric parameters
were not independently normalized, and the event/frame outputs remain
postcapture diagnostics. No scientific failure was registered because the
physical theorem was not evaluated. These controls protect the type
boundaries in the displayed spine.
