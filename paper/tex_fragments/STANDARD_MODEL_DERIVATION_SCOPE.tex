\subsection{Gauge-chain theorem and falsification boundary}

The finite carrier calculation derives the incidence polynomial for \(J\),
the target-blind response \(R=-J\), and its relative sector signs. Complete
reversible port response and endogenous overlap transport force the compact
Lie type
\(\mathfrak{su}(3)\oplus\mathfrak{su}(2)\oplus\mathfrak u(1)\).
The released matrix current is an exact conditional realization. Given that
realization, the declared fermionic Spin category, and rank-15 matter
projector pair,
the matter certificate derives anomaly balance, exact hypercharge up to
conjugation, three colors, the common \(\mathbb Z_6\) tensor kernel, and the
maximal faithful matter image. The Lie-type theorem and the conditional
matter theorem have separate premise sets.

Four central global forms act consistently on the same local tensors: the
cover and its \(\mathbb Z_2\), \(\mathbb Z_3\), and \(\mathbb Z_6\)
quotients. The kernel theorem identifies the maximal faithful local matter
image. The six-axis artifact computes an order-six flux menu after its
coefficient relations are declared. This conditional menu matches the
\(\mathbb Z_6\) quotient and carries the declared matter single-valuedly. It
does not select the physical global form. Source selection of the relation
lattice, character-category completeness, and a same-source loop-to-kernel
identity are work in progress. Laboratory identification of the current and
flux sectors, four-dimensional instanton normalization, theta
periodicity, monopole dynamics, equality between the finite current and the
categorically reconstructed group, scalar attachment and multiplicity,
physical matter-pole and continuum identification of the conditional
rank-\(45\) candidate, selection of the local matter action, cross-domain
Spin transport, and quantum field realization are work in progress.

The order-six seam classification keeps two actions separate. Every
pure-hypercharge character is enumerated: the fixed-space dimension menus are
\(\{15,7\}\) at order two, \(\{15,3\}\) at order three, and
\(\{15,7,3,1\}\) at order six. Particular nontrivial characters give the
dimensions \(7\), \(3\), and \(1\). The canonical diagonal
\(\mathbb Z_6\) quotient kernel acts trivially on all fifteen realized matter
states. These are different representations of the same abstract cyclic
order. Refinement preserves the classified actions. The finite calculation
selects no physical seam character, flux action, or 2-representation, and it
does not exhaust every possible seam grammar.

The gauge result fails if the incidence identity, target-blind response,
equivariant lift, anomaly scan, tensor-character enumeration, or refinement
persistence receipt fails its registered negative controls. The categorical
route fails outside its combined strictification, trivial-holonomy,
localization, and refinement/fiber receipts.

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
