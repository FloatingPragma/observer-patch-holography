% Shared closure-principle panel. Included by papers that state the full
% axiom basis, immediately after THREE_AXIOM_BASIS.tex. This states a
% modeling principle on the axiom class, not a fourth axiom; every surface
% counting the basis still counts exactly three axioms. Reviewed reference:
% docs/AXIOM_REFERENCE.md.

\paragraph{The closure principle.}
The axioms describe the observer screen; the modeling stance adds that the
simulating and the simulated description are one system. Every quantity that
has both a construction-side reading and a readback-side reading then
carries two requirements: the readback return map must close on a unique
fixed point on its declared domain (consistency), and the two readings must
be readings of one invariant (identity), where each identity is a
separately typed physical attachment rather than a free move. Two closures
are constructed. The local screen-grain closure
\[
P=\varphi+\sqrt{\pi}/A_T(P),
\]
with \(A_T(P)\) the inverse coupling returned by the declared source map,
has one interval-certified fixed point on its physical domain; its
laboratory attachment awaits a same-scheme hadronic spectral transport, and
the comparison with the measured fine-structure constant stands at the
few-parts-per-million level as a source-map result. The global capacity
closure is an additional named source law: the axioms alone are proved not
to force a unique capacity law, and the current candidate corrects the
electroweak bridge capacity by a closure overhead,
\[
\ln(N/\pi)=\frac{6\pi}{P\,\alpha_U(P)}-\frac{P}{24},
\]
with \(\alpha_U(P)\) the unified gauge width fixed by the pixel-closure
equation and the overhead carried by the shared-cut \(\mathbb Z_6\)
protected-reserve mean. Four named premises (invariant seam implementation,
interface completeness, seam multiplicity one, seam record content) and a
horizon-record attachment remain open, and the candidate's agreement with
the measured cosmological constant is retrospective. The closure class is
falsifiable as a whole: a measured capacity above the uncorrected bridge
value \(\pi\exp\!\left(6\pi/(P\,\alpha_U)\right)\) rejects every
positive-overhead completion.
