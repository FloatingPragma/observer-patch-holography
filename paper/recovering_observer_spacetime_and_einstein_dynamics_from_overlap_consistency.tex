\documentclass[11pt]{article}
\input{tex_fragments/DERIVATION_PAPER_PREAMBLE.tex}

\title{Recovering Observer Spacetime and Einstein Dynamics\\
from Overlap Consistency}
\author{B.\ M\"uller \and Alexander Osika \and Mario Poneder \and Kai Xue
\and Peter Nguyen \and Maarten Antoine Visser \and David Matscheko}
\date{\OPHPaperReleaseDate}

\begin{document}
\maketitle
\OPHPaperReleaseBanner

\begin{center}
\small\textbf{Author affiliations:} Bernhard Mueller, Pragma Research Inc.; Alexander Osika, EtherWorks.
\end{center}

\begin{abstract}
Observer-Patch Holography starts from finite patches that record, read shared
boundaries, and repair disagreement. This paper develops its conditional
spacetime and gravity chain. On a declared twelve-port branch, the normalized
repair-response metric has an exact rank-three quotient. The antipodal load
readout reaches every signed six-coordinate record, while conservative seam
currents span its even-sum submodule. Both are dense in the same abstract
continuous Euclidean completion under the response metric. Physical position,
scale, and overlap gluing enter only as premises. A separate incidence limit gives a
round celestial two-sphere; modular covariance gives Lorentz-frame kinematics and
the three-dimensional observer-frame space. Record germs yield a
four-dimensional Lorentzian event manifold only under named
population, separation, response-chart, cone, and causal conditions. A
machine-checked finite soldering contract isolates the algebraic part: an
actual coincidence setoid and invariant readback give a unique descended
readback. Separately, one affine Lorentz cocycle and a supplied chart-coordinate
family satisfying its overlap law induce displacement, celestial, frame, and
local-rest-space covariance. The formal contract does not identify that chart
family with the quotient-descended readback. It retains the source, population,
certified-separation, open-chart, physical-cone, refinement, causal, and clock receipts.
Authenticated read-from provenance supplies an exact finite informational
order. Separately, one target-clean finite source replay places that order,
capture-specific atlas candidates, seam transport, and a local operator domain
on the same finite carrier. Neither result constructs physical causal order or
the continuum manifold.
Positive null translations supply directional charges, and finite null
tomography reconstructs a tensor when its linearity relations hold.  The local,
covariant, conserved stress reading additionally requires same-source
null-stress matching, locality/covariance, and a weak Ward balance.  A declared
fixed-cap generalized-entropy stationarity interface, together with the proved
finite MaxEnt identities, gives the Einstein equation only under the remaining
small-ball, common-scaling, universal-coupling, vacuum-reference, and independent
scale-readout premises. Fixed-screen identities determine the
capacity-transfer law and de Sitter shock sign. The physical reading requires
one refinement tower carrying every continuum condition.
\end{abstract}

\section*{Derivation map}

The exact local carrier branch is
\[
\begin{aligned}
&\text{signed cumulative port records and normalized repair response}\\
&\hspace{3em}\Longrightarrow
\text{abstract continuous three-dimensional Euclidean carrier}.
\end{aligned}
\]
The distinct global spacetime implication chain is
\[
\begin{split}
&\text{independently typed finite inputs}\\
&\quad=
\left\{
\begin{aligned}
&\text{source-derived authenticated informational precedence},\\
&\text{exact rank-three local response carrier},\\
&\substack{\text{algebraic }S^2\text{/null-ray bridge plus supplied}\\
\text{atlas, cone, and per-event Lorentz frames}},\\
&\substack{\text{supplied populated-event and count-density calibration}\\
\text{distinct from public capacity }N}
\end{aligned}
\right.\\
&\quad\xRightarrow{\text{supplied compatibility data}}
\text{decorated finite causal candidate}\\
&\quad\Longrightarrow
\substack{\text{refinement and physical cone/order faithfulness}\\
\text{plus manifoldlikeness, dimension, and count--volume receipts}}\\
&\quad\Longrightarrow\text{conditional four-dimensional event manifold}\\
&\quad\Longrightarrow\text{positive translations and finite null tomography}\\
&\quad\Longrightarrow\text{conditional local conserved }
   \langle T_{ab}\rangle\\
&\quad\Longrightarrow
G_{ab}+\Lambda g_{ab}=8\pi G\langle T_{ab}\rangle .
\end{split}
\]
The informational-precedence lane is causal-set-like only in the mathematical
sense that it is a finite, hence locally finite, partial
order~\cite{bombelli1987,surya2019}. Standard causal set theory additionally
interprets order as physical causal precedence and element number as volume.
A faithful causal embedding, manifoldlikeness, dimension, topology, and a
count-to-volume law remain separate requirements
\cite{bombellimeyer1989,brightwellgregory1991,majorrideoutsurya2007}; stable
homology is necessary rather than sufficient evidence for
manifoldlikeness~\cite{majorrideoutsurya2009}. Under the appropriate continuum
causality hypotheses, causal order fixes conformal geometry and volume fixes
the conformal factor~\cite{hawkingkingmccarthy1976,malament1977}. Standard
Lorentz-invariant causal-set approximation uses Poisson
sprinkling~\cite{bombellihensonsorkin2009}. The deterministic OPH commit log is
not a Poisson sprinkling, and this finite order proves neither a continuum
limit nor a causal-set Hauptvermutung.
The four inputs in the first display remain independently typed: neither the
rank-three carrier nor the Lorentz frame fiber is the event dimension, public
capacity \(N\) is not event number, and the event-population count is not a
spacetime volume without calibration. The decorated
finite object therefore does not constitute spacetime; the conditional continuum is
emergent rather than fundamental. The exact finite bridge
\(\mathsf{SourceUnitDirection}\simeq S^2\simeq
\mathsf{FutureNullRay}\) identifies the unit directions of the rank-three
source Gram quotient with algebraic celestial and future-null-ray labels. The
formal compatibility packet has no constructed inhabitant and carries no
count-density field; its atlas, cone, and per-event frames are supplied. It
does not identify the directions with a physical sky or signal. The four-dimensional
reading requires the physically faithful images of authenticated local read-from
links to cover those directions densely and isotropically, because null directions in Lorentzian
dimension \(d\) form \(S^{d-2}\). That coverage is not supplied. The measure
lane independently requires \(\#I/\rho\to\operatorname{Vol}(I)\). Refinement
must preserve order, directions, and count density while soldering rank-three
neighborhoods with \(\lvert B_{\mathrm{car}}(r)\rvert\asymp r^3\) over a
scaling window. This is a compatibility condition on the four inputs, not a
fifth input. The seeded global finite carrier wiring therefore does not
inherit the local band dimension.

The machine-checked finite composition has a precise scope.
\texttt{SourceOrderFrameCompatibilityPacket.finiteConsequences} proves, for
each supplied packet, generated-order/future-cone agreement in every supplied
chart, the rank-three quotient and rest fibres, the exact
\(\mathsf{SourceUnitDirection}\simeq S^2\simeq
\mathsf{FutureNullRay}\) equivalences, and preservation of the quotient Gram
form by supplied frame transports. The packet itself has no constructed
inhabitant. Population, separation, physical causal faithfulness, dense and
isotropic link-direction coverage, faithful embedding or manifoldlikeness,
count--volume calibration, and compatible refinement therefore remain
testable inputs to the conditional event-manifold theorem.

The stronger exact tensor composition is
\texttt{sourceOrderEinstein\_from\_source\_directions}. Its
\texttt{SourceIndexedEinsteinPremises} place supplied symmetric geometry and
stress fields on the same finite event carrier as the source packet. They ask
for balance only on nine fixed algebraic source-direction representatives,
the inverse images of the existing coordinate-fixed null-tomography frame,
plus discrete Ward and Bianchi identities, symmetric-step connectedness,
vacuum reference, and scale identification. The proved
\(\mathsf{SourceUnitDirection}\simeq S^2\) map carries those nine directions
into the exact null-tomography frame. The theorem then derives the all-null
balance, pointwise and constant metric ambiguities, and the finite-carrier
Einstein-form identity
\[
 \mathsf{geometry}_{ab}=8\pi G\,\mathsf{stress}_{ab}+\Lambda\eta_{ab}.
\]
The all-null balance is therefore removed as an independent hypothesis. This
distinguished nine-set is neither selected by the informational poset/source
dynamics nor invariant as a set under Lorentz or \(\mathrm{SO}(3)\)
transformations. This
tensor calculation uses the independent source Gram quotient's nine algebraic
directions, not the generated order. The packet's finite-order consequences
share the event type, but no provenance link selects a direction, tensor
field, step, or balance, and no packet or premise inhabitant is constructed.
The matrix fields, four step maps, and \(\eta_3\) belong to a supplied
$3+1$-dimensional tensor interface. The theorem proves compatibility and a
premise reduction inside that interface; it does not derive event dimension
from the poset.
The premise bundle also carries a universal-source equality into the
conclusion for downstream composition, but that equality is not used in the
displayed tensor calculation.
The adapter \texttt{toContinuumEinsteinPremises} has an inherited type name;
it is not a continuum theorem. This
identity is exact algebra on supplied fields; it does not prove that
\(\mathsf{geometry}\) is smooth Einstein curvature or that
\(\mathsf{stress}\) is physical stress-energy. The event-manifold,
link-direction, refinement, count--volume, small-ball, and continuum
identification receipts remain required for that reading. On an inhabited
common-domain tower, \texttt{composedEinsteinBranch} gives the conditional
Einstein-form composition from its typed readout, first-law, MaxEnt, scaling,
conservation, vacuum, coupling, and scale premises. A smooth physical reading
additionally needs the external event-manifold, refinement, count--volume, and
geometric-identification receipts just listed. Neither the source packet nor
the tower is constructed.

A separate post-hoc constructive control imposes FCC rank-three gluing with
twelve cuboctahedral \(O_h\) ports, exact versioned provenance, and nested
identity-hash induced orders at depths \(8,12,16,20\). At depth \(20\), its
three frozen thinnings retain \(2{,}583\)--\(2{,}746\) events, with ordering
fractions \(0.10115\)--\(0.10786\). Their flat-control Myrheim--Meyer
candidates \(3.910\)--\(3.986\) are inversions of those fractions, not
independent evidence~\cite{reid2003}. The fixture has exact nested orders; on the checked
grids its spatial carrier-ball counts match the cubic formula for
\(0\leq r\leq20\) and its fine-carrier counts match the quartic formula for
\(1\leq n\leq10\) at imposed depth \(2n\)~\cite{ophfpe}. These are
only bounded depth-\(20\) values and bounded count matches for an imposed
architecture, not an all-radius or all-depth theorem. Its post-hoc analytic-profile trend over
depths \(8\)--\(20\) uses interval abundance, a standard causal-set locality
diagnostic~\cite{glasersurya2013}, and reverses for every seed in an additional
out-of-family depth-\(24\) extrapolation control~\cite{ophfpe}. At depth \(20\) its matched-Minkowski
profile RMS is \(0.090083\), versus \(0.002423\) between independent controls,
a factor of \(37.18\). Held-out confirmation remains unperformed. Its count-height slope
\(3.3867\) and \(H/\lvert I\rvert^{1/4}\) normalization also fail the
four-dimensional scaling checks. External depth is neither physical volume nor
poset height. The gluing and thinning are imposed and the current federation
selects neither; fixed \(O_h\) ports are not an \(A_5\)-compatible refinement
family becoming dense and isotropic on \(S^2\). This is not finite causet similarity, refinement convergence,
manifoldlikeness, or a physical causet, volume, Lorentzian, or continuum
result.
Each arrow below states its own finite,
analytic, scaling, or physical
antecedents. In particular, the local-stress arrow consumes same-source
null-stress matching, locality/covariance, and the weak Ward balance; finite
null tomography does not supply them. The celestial \(S^2\) and the frame hyperboloid do not by
themselves populate a spacetime. The event-manifold and Einstein conclusions
are conditional theorems on their named receipts. Of the local-stress
inputs, one finite precursor is in hand at one energy component: a
face-local discrete Poynting law on the committed twelve-port carrier, exact
at every step of the scaled Maxwell evolution, whose face sum is the
committed global energy balance and whose density--flux pair is one
conserved current in the source-free case
(\path{Lean/Screen/LocalEnergyBalance.lean}). It supplies no momentum or
stress components, covariance, or continuum limit, and the identification
of the screen field energy with the modular energy the composition consumes
is read against one declared clause: the clock-and-energy calibration row
of the four-law surface, whose reference is the Gibbs state of a declared
energy at one declared inverse temperature \(\beta\). That row is inhabited
by the screen field energy, and against it the modular energy ledger of any
state is exactly \(\beta\) times its field-energy expectation plus
\(\log Z\), the cap first law takes the Clausius form
\(S(p)-S(\tau)=\beta(\langle H\rangle_p-\langle H\rangle_\tau)-D(p\Vert\tau)\)
with the offset cancelled, the slope and the reference are determined by
the ledger, the energy is free exactly up to an additive constant, and
\(\beta\) is determined by the reference once the family and the step are
declared; a two-point reference outside the Gibbs form has equal energy
expectations with unequal ledgers, so the affine reading is specific to
that clause. Nothing derives that the record reference is Gibbs and nothing
selects \(\beta\); the identification stays conditional on the declared
row (\path{Lean/Thermodynamics/GibbsReferenceEnergyIdentification.lean}).
The collar modular surface does not supply that inverse temperature. Its
one thermal object, the reference on the realized branch of the declared
maximum-entropy constraint, with modular Hamiltonian \(K=\lambda\cdot S\)
on the four joint sectors, has spectral
weights equal to the Gibbs state of \(K\) at \(\beta=1\) in modular units,
and for every nonzero \(\beta\) they equal the Gibbs state of \(K/\beta\)
at \(\beta\), so every temperature declaration inhabits the calibration row
for the collar reference and two declarations with different \(\beta\)
both do; a declared energy with two distinct levels pins \(\beta\), a
declared \(\beta\) pins the energy up to a constant, and the collar
reference is Gibbs for an energy \(H\) at \(\beta\) exactly when
\(K=\beta H+\mathrm{const}\). The collar functional is KMS at parameter one
for the inner flow of \(K\) and at parameter \(\beta\) for the flow of
\(K/\beta\), so the KMS parameter and the inverse temperature trade
against the energy scale. The identification of the collar weights with
the record reference of the cap first law stays declared
(\path{Lean/Thermodynamics/CollarTemperatureReading.lean}).

The sourced field equations on the committed complex are consistent in the
following exact sense. The hopping load and hopping current of a declared
port path satisfy the committed continuity equation at every port and every
step, and a finite family of hopping charges satisfies it with total load
equal to the sum of the charges. Along the scaled Amp\`ere evolution with any
current, the Gauss constraint propagates from one step to the next exactly
when the sources satisfy the continuity equation, so a field history that
solves the Amp\`ere evolution with a family of hopping charges and starts from
Gauss-consistent data stays Gauss-consistent at every step. The complete
criterion is an equivalence: for declared sources, a field history solving
the Amp\`ere evolution and the Gauss constraint at every step exists if and
only if the initial load is neutral and the sources are conserved; the
existence direction is an explicit second-order recursion in the temporal
gauge started from the canonical Coulomb field of the initial load. Because
the port boundary conserves total load on the closed carrier, one hopping
charge alone has a Gauss-consistent history only when its charge is zero; a
join of a worldline to the field sector therefore carries a neutral partner
or a neutralising background. A neutral pair of crossing charges, with
integer Green potential checked against the committed Green and incidence
tables by a kernel check, inhabits the scaled Maxwell bundle with a nonzero
current, and the unit crossing of every seam satisfies the continuity
equation by a kernel check that fixes the sign convention between the
crossing current and the incidence operator
(\path{Lean/Screen/SeamChargeContinuity.lean}).  The neutral-partner route is
also realized in one finite action rather than by appending an independently
prescribed background after variation.  For $h\ne0$, the source-free
Maxwell window plus the two opposite minimal couplings and two clock actions
is invariant under endpoint-vanishing gauge functions.  With the
seam-potential endpoints fixed, its field variations are equivalent to the
interior sourced Amp\`ere equations at $m<N$ and Gauss equations at
$n<N+1$.  Replacing either path over a closed two-step interval while
fixing the fields and other path gives an exact off-shell action difference,
not by itself a path equation of motion.  At $q=1$, $h=1/2$, the explicit
Coulomb-started packet is stationary in the field slots, but for every
$N\ge1$ delaying either its positive seam-$0$ crossing or its negative
seam-$29$ crossing, while fixing the fields and the other path, changes the
same action by $5/12$ for every pair of clock-unit values within the fixed
quadratic clock-action form.  The charge-conjugate results therefore fail the
declared closed-two-step exchange-stationarity condition in both path slots;
applying both canonical exchanges simultaneously changes the action by
$5/6$.  These are only the displayed discrete exchanges, not continuum
first-variation claims or a no-go for other paths, fields, or action shapes.
The counterexample is specific to that Coulomb start.  On the window $N=1$,
a second exact rational temporal-gauge history with the same crossings solves
the sourced equations and leaves the same action unchanged under every
in-window closed two-step replacement of either path.  The exhaustive route
check has two zero-difference endpoint routes and two triangular detours whose
clock difference $-4$ is cancelled by interaction difference $+4$.  The
sources are nonzero and opposite.  At the separately declared carrier unit
$\tau=3$, both crossings are timelike; identifying $\tau=h$ would instead make
a crossing spacelike
(\path{Lean/Screen/NeutralPairCoupledAction.lean},
\path{Lean/Screen/NeutralPairJointStationaryWitness.lean}).  The step, charges,
paths, rational history, clock unit, and action shapes are declared; the sign
of the committed load relative to a physical charge, source selection,
boundedness of the sourced history, a minimum or stability theorem, laboratory
calibration, and any continuum limit are outside this statement.

A second three-dimensional construction is local and Euclidean. For the
declared twelve-port repair mean, the centered equal-port response kernel has
an exact spectral decomposition. Dividing by its slow response before taking
the infinite-step limit selects a rank-three projector. Its range in the
twelve-port counting space, equipped with the induced Gram form, is an
intrinsic Euclidean carrier. On the conditional signed cumulative
port-record/load module, the limiting form has a three-dimensional radical on
the real six-control extension. The integer module has no nonzero metric
kernel, embeds densely in the resulting quotient, and completes to the same
abstract continuous carrier, up to the unique isometry that preserves the
port labels. The source load map is exactly onto \(\mathbb Z^6\), with the
antipodally equal loads as its kernel. Conservative seam boundaries have
image
\[
 D_6=\{z\in\mathbb Z^6:\textstyle\sum_i z_i\equiv0\pmod 2\},
\]
with Smith invariants \((1,1,1,1,1,2)\). The pullback response metric makes
this submodule dense in the same completion; the ordinary six-dimensional
lattice metric on \(D_6\) is a different metric and is not used. Cumulative
record addition acts simply transitively on the exact record carrier and by
isometries on its completion. The sixty directed seam labels form one proper
carrier-rotation orbit and give the corresponding homogeneous internal
convolution under the stated naturality and unique-minimizer premises.
The thirteen finite covariance/isometry table identities in the proper-action
subpacket are checked with Lean's \texttt{native\_decide}; they consequently
trust the native compiler and runtime as well as the kernel and are not
kernel-only receipts.

This order is essential:
every finite-step kernel has rank eleven on the centered space and rank six on
the signed sector. The construction supplies a local carrier-position
readback. It supplies no global spacetime, preferred Cartesian frame,
physical scale, or physical translation. It supplies no source-selected repair
law or readback topology, no overlap/refinement gluing, and no identification
of a seam record with physical propagation.

The Lorentz arrow is a composition with two independent inputs on one
refinement tower: the finite cap-normal Bisognano--Wichmann certificate,
whose clauses are cap-normal density and nondegeneracy, BW framing,
support-order faithfulness, geometric support-flow group and continuity
control, held-out oriented cross-ratio convergence, independently normalized
geometric \(2\pi\)-KMS comparison, and wrong-normalization separation, and an
independently produced complete multiresolution algebra-state comparison
package on the same tower. Geometry does not manufacture the modular state,
and bare finite consensus implies neither input. The Einstein arrow consumes
one common scaling family, universal coupling, a vacuum reference, and
independent scale readouts.

The finite source realization sits below the event-manifold arrow. It supplies
one exact informational event complex, a closed observer-neighborhood cover with affine
wiring transitions, a seam-sign layer, and a local operator domain. An
isolated rerun of the same producers reproduces canonical receipt content. The
finite object lacks the uniform local Lorentz signature, open-chart topology,
physical causal attachment, and continuum identifications required by that arrow.

The formal soldering layer does not repair this deficit. It proves that, once
one genuine coincidence quotient and invariant raw readback are supplied, the
readback descends uniquely. Separately, after a time-oriented affine Lorentz
overlap cocycle and chart-coordinate family obeying its overlap law are
supplied, coordinate differences discard translations, Lorentz intervals are
chart independent, future-null rays give covariant celestial directions, and
observer-frame rest readbacks transport isometrically. The formal theorem does
not identify that coordinate family with the quotient-descended readback. The
source rank-three Gram quotient is isometric to one
standard internal rest fiber only as a candidate local readout. It is not
absolute space, a populated event base, or a clock.

The bounded observer-time continuation proves the sharp interface boundary.
A natural-number-ranked record order admits arbitrary strictly increasing regradings, including an
explicit three-tick nonlinear clock that is not affine-equivalent to its rank
clock. Only after a unit-timelike affine coordinate law is supplied does a
positive clock increment along that same supplied affine history become
additive dimensionless proper time whose square is the chart-invariant Lorentz
quadratic interval. One common event leaves affine rate and origin
underdetermined. Two ordered common events determine the unique
positive-affine interpolation of their four readings. A distinct third event
and distinct readings give a nondegenerate cross-multiplication check with no
new fit parameter. A held-out interpretation additionally needs a
predesignation and custody protocol. The passing affine and failing cubic
controls use separate clock-generated atlases and therefore certify the
algebraic comparison only. They do not construct a physical clock pair on one
calibrated geometry. A separate three-record, two-chart witness inhabits the
geometric interface. The formal package does
not produce the history, a coherent network of three or more clocks, the
calibration from the OPH source tower, or an SI clock.

A kinematic mass-shell packet is machine-checked on the declared Hermitian
Lorentz module (\texttt{Lean/Geometry/MassShellKinematics.lean}). For one
declared mass parameter \(m\) and any future unit-timelike frame \(u\), the
scaled vector \(p = m\,u\) has Lorentz square exactly \(m^{2}\), and its
scalar coordinate obeys \(p_{0}^{2} = m^{2} + \lvert\mathbf{p}\rvert^{2}\).
For positive \(m\) the scalar coordinate dominates \(m\), with equality
exactly when the spatial coordinates vanish, and at the standard frame it
equals \(m\). Every oriented Lorentz map carries the construction to the
transported frame with the same Lorentz square, so the shell identity holds
in every oriented chart, and a future null vector satisfies the zero-mass
identity \(v_{0}^{2} = \lvert\mathbf{v}\rvert^{2}\). The mass parameter is a
declared hypothesis on the declared module, stated in its dimensionless
convention with unit invariant speed. The packet identifies no coordinate
with a physical energy readout and makes no physical rest-energy claim: the
clock and energy calibration anchors and the observer-to-physical-spacetime
attachment are not supplied.

A separate declared finite region order admits a cone-compatible embedding
(\path{Lean/Geometry/CausalOrderComposition.lean}). The determinant
quadratic form orders the declared Hermitian module: one coordinate precedes
another when their difference has nonnegative Lorentz square and nonnegative
scalar coordinate, and this relation is a partial order whose transitivity
is the reverse triangle inequality, proved from the three-coordinate
Lagrange identity. A declared vertex assignment places the six-element
region lattice of the rich-fibre observer net inside the module so that the
committed region order agrees with the cone order of the images, in both
directions on the whole carrier. The assignment is injective, the image
contains two timelike-related and two spacelike-unrelated pairs, declared
disjointness of regions lands in spacelike separation, and composing with
any oriented Lorentz map, or with any affine overlap transition, preserves
the equivalence; both invariances are clauses of the single composed
receipt. The two-site partition net composes through a label map with the
same receipts; its four-element carrier has exactly one incomparable pair,
with both directions machine-checked: the pair of singletons is
incomparable, and every incomparable pair is that one, which bounds the
spacelike content any faithful placement of that carrier can show. The
vertex assignment is a declared witness: a second distinct assignment
satisfies the same equivalence, so the committed data does not force the
embedding. No physical causal order, spacetime,
clock, or observer frame is attached; those identifications are additional
premises not supplied here.

A separate finite-graph precursor makes the discrete inverse-square route
premise-complete.  For a supplied integer layering, steady source law, shell
equidistribution, and shell count $c n^2$ through a finite depth, exact
divergence bookkeeping gives outward flux per shell edge
$Q/(c n^2)$, with $n^2$ times that flux independent of the shell.  An
explicit three-vertex chain inhabits the premises.  The same formalization
also proves why the depth cutoff cannot be hidden: on a closed finite carrier,
a globally steady drained source has zero total load, while an $n^2$ shell
count imposed at every positive layer forces $c=0$.  These are finite
conditional identities, not an identification of layer with physical radius,
flux with gravity, or source with mass density.

\tableofcontents
\clearpage

\def\OPHSkipStandardModelChain{1}
\def\OPHEinsteinDerivationPaper{1}
\def\OPHSkipEmbeddedReferences{1}
\input{tex_fragments/PAPER.tex}
\appendix
\input{tex_fragments/DERIVATION_TECHNICAL_SUPPLEMENT_PORT.tex}
\input{tex_fragments/OBSERVERS_REFERENCES.tex}

\section*{AI Assistance Disclosure}
This research project used research-grade commercial models, including
Anthropic's Fable and OpenAI's GPT-5.6-Sol, for research support, software
development, editing, and synthesis. The authors are responsible for the
paper's claims, methods, and final text.

\end{document}
