# Complete routed record-read histories

These are new executions on the declared W12 L4/L5 support, not the earlier
logical-family census. The baseline and centre-source +1 intervention are
retained at each q. Each event has an explicit 64-byte row; lossless
differential segments recover every original field and preserve its decoded
SHA-256. Part files are at most 32 MiB. The original declared-family receipts
in the parent directory remain unchanged.

The compiler, independent replay, finite Lean theorem, resource model and
reproduction commands are in
[`code/source_read_routing/README.md`](../../../code/source_read_routing/README.md).
Its `specification.json` retains M1 explicitly and assigns its source
derivation to #779/#740 through PR-52. No physical clock, quantum instrument,
four-volume interpretation of routing events, or A1--A3 derivation is claimed.

The inventory in the parent `archive_manifest.json` includes these new files
as a separately identified research-repository extension. That inventory
attests their stored bytes; the full native verifier checks their scientific
contents. The small control oracle and semantic mutation tests also execute
in CI.
