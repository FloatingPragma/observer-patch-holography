# The OPH Proof Spine

The end-to-end structure of the proof strategy, with the status of every step and a
complete register of open gaps. This is the master audit document of the docs/ set.
The three axioms and the late strange-loop closure hypothesis are stated in the
[axiom reference](AXIOM_REFERENCE.md), the repository [README](../README.md), and the papers; each quantitative closure
condition is tracked as a
[GitHub issue labeled `closure`](https://github.com/FloatingPragma/observer-patch-holography/issues?q=is%3Aissue+label%3Aclosure);
the typed carrier and cross-paper composition boundaries are stated in the
papers and the canonical claim registry.

<!-- CLAIM-STATUS-PROJECTION:BEGIN -->
## Canonical claims and live-gates projection

Generated by `tools/build_scoreboard.py`; do not edit this block. Its SHA-256 commits to every complete ordered canonical claim row (including class, status, and gates)
and to the complete open-issue-number set in the committed GitHub ledger. The generated [claims scoreboard](../tracking/claims_scoreboard.md) is the row-by-row view.

- Release: `r2016`
- Projection SHA-256: `6efbb49c1d1a8b97aea1d6ddde291383a589684cc7c101f99ea4a123e71b504c`
- Registry: 90 canonical claims (`physical_establishment` 0, `empirical_implementation` 4, `emitted_artifact` 5, `branch_entry` 1, `conditional_implication` 69, `declared_structure` 11)
- Claim gates: 18 distinct open issues across 37 gated claims
- Open-problem ledger: 21 open issues
- Undischarged physical identifications: 13
- `compression_bound: not_computable - menu sizes undeclared (#694, #696, #697)`
<!-- CLAIM-STATUS-PROJECTION:END -->

## The carrier doctrine that binds the stack

OPH is presentation-invariant, not carrier-architecture-neutral. Relabeling a
port, changing hidden coordinates, or replacing one implementation by another
is physically silent only when the full observer-facing signature is
preserved: port number, oriented incidence, accessible algebra, response,
repair law, records, clock data, and refinement maps. Changing that signature
can change the admitted physics. The twelve-port Echosahedral architecture is
therefore allowed to matter; its arbitrary labels and hidden presentation are
not.

Three objects called a screen must remain typed throughout the paper stack:

1. the **local carrier boundary**, the oriented twelve-port interface of one
   Echosahedral carrier;
2. the **federation screen**, the routed network of carriers, observer
   supports, public records, and accepted repairs;
3. the **global support screen**, the refinement-limit support chart that is
   $S^2$ on the Lorentz/BW branch.

One carrier is not automatically one primitive observer or one support-chart
cell. A carrier or connected subfederation becomes an operational observer
only after readback, durable record, feedback or repair, prediction/control,
and checkpoint continuation are supplied. Likewise, phase locking can propose
or stabilize candidate repairs, but it implies consensus only after a typed
bridge proves accepted descent, local diamonds, fairness where needed, and
the same quotient normal form. Local $A_5$ incidence does not by itself
produce the global $S^2$ support.

The BW dependency has two inputs. `FiniteCapBWCertificate`
contains the support, cap-flow, orientation, group-law, continuity, and
normalization clauses. `MGNS-1` independently supplies the complete
algebra-state modular representation. BW consumes both on the same refinement
tower. Geometry does not manufacture the modular state. The composition
contract and finite GNS-tower certification closed historically as #308 and
#574. The live same-tower MGNS, continuum, and small-diamond premises belong
to F1 (#694); structural net inheritance is separately gated by E4 (#701).
The archived #592 H3/KMS campaign remains historical evidence, not a live
owner.

## Where the spine is strong

Five load-bearing assets anchor the spine. First, interval certificates: each
declared P map has exactly one fixed point on its stated domain, and the
conditional electroweak comparison map has an interval-certified fixed point; a
hostile third-party audit reproduced this arithmetic and found
no false theorem in the recovered mathematical core across its 42 findings.
Second, machine checking: a sorry-free Lean theorem subset covers the consensus
core and the capacity-coupling algebra. Third, fail-closed execution: the
hadronic coordinate algebra and finite-clock eligibility predicate have
fail-closed gates and adversarial regressions. Fourth, the falsification
program is restricted to mature non-cosmological claims and contains no
target registry or forward scorecard. Fifth, proximity: the gauge-width map lands
2.5×10⁻⁶ relative from the measured α⁻¹ with the transport term open; the
exact convention map distinguishes the PDG 2026 energy-pole mass
\(M=\operatorname{Re}\sqrt{s}\) from the legacy
\(\sqrt{\operatorname{Re}s}\) coordinate, while the OPH chart-to-pole readout
contract and theory covariance are open; and the conditional electroweak comparison coordinate sits
about 2.5 one-dimensional Planck standard deviations from the Λ-located
coordinate. Neither constructs the operational readback map. The statuses below are binding and move
only by dated artifact.

## The claim, in two parts

**Part A (the loop-closure program).** The two declared pixel maps each have one
arithmetic fixed point on their stated domain. Their physical Thomson map is incomplete.
The direct finite readback is the stable public-record code size of the
descended semantic checkpoint system. Public descent, the dimension bound,
saturation rigidity, greatest-fixed-point selection, refinement stabilization,
and capacity-density implications are closed under their stated premises. No
physical producer and receipt bundle for that map has landed.

**Part B (the conditional structure program).** Given $(P,N)$, the three
axioms, the closure hypothesis, and the named branch receipts, the declared recovered
structures follow. The exterior-algebra Standard Model witness is closed under
its carrier assumptions; forcing that package from the physical twelve-port
screen is open.

Part A closed by landed identification and a singleton fixed-point receipt,
plus Part B closed by discharged gates, is the formal proof that our universe
is the strange loop's unique solution. A greatest-fixed-point convention alone
selects one self-sustaining capacity and does not prove that smaller fixed
points are absent. Nothing short of the full combination is claimed as proof.

## Part A, step by step

The observers synthesis paper owns the canonical $P/N$ closure construction.
The two specialist derivation papers record the typed theorem boundaries, and the particle paper
owns downstream electroweak and mass bridges.

Step labels A1-A13 and B1-B10 index proof steps of this spine. They are not
axiom labels: the three axioms are always cited as Axiom 1, Axiom 2, and
Axiom 3.

| Step | Content | Status |
|---|---|---|
| A1 | The three axioms and the strange-loop closure hypothesis stated once, with claim rules separating theory, test, and working layers | done: repository README and the observers synthesis paper |
| A2 | The two closure equations defined: $P=\varphi+\sqrt\pi/A_T(P)$ and $\mathfrak F_{r,0}(D_\star)=\{D_\star\}$ with $N_\star=\log D_\star$ | formal equations stated; the physical $A_T$ transport is open. Direct $N$ is not evaluable on the incomplete source antecedent and supplies no cosmic value |
| A3 | Uniqueness of P | PROVEN only for each declared incomplete arithmetic map on its stated domain; this does not certify the missing physical Thomson map |
| A4 | Public record gluing | PROVEN: total atom readout maps define finite compatible global sections and their commutative function algebra. A source-derived fixed-cutoff simulator packet at $D=24$ verifies the finite evaluator contract inside its declared source category. Exact code checks finite incidence, action, projection, composition, extension, and oriented-record fiber-product controls for an all-rung counterfamily. An independent simulator replays the finite sample graphs. Lean proves fixed-set disagreement for the typed base-agreement, positivity, and carrier-bound completion structure. Transported source-contract controls are checked on rungs one through six; universal all-rung membership and the executable-to-Lean membership bridge are open. The independent finite $A_5$ control has $M_0=60$ and $D_{\rm raw}=60k$; publicly inert multiplicity blocks its raw equality from physical promotion. |
| A5 | Correctable public record capacity | Official universe equation: $N=\log M_0(\mathfrak U_N)$. Here $M_0$ is always a multiplicative code size. The finite definition uses the compound graph of source-supplied global joint checkpoint kernels: $M_0(q)=\alpha(G_q)$. Proven implications include the support criterion, finite semigroup closure, reversible fast branch, total-variation robustness, $M_\varepsilon\le D$, and complete rank-one saturation. Cyclic permutation and local-marginal countermodels exclude fixed-projector and marginal-only substitutes |
| A6 | Terminal-fiber readback | PROVEN set-valued construction $\mathfrak F_{r,\varepsilon}(D)=\{M_\varepsilon(q)\}$; a scalar map exists only on a complete nonempty fiber with singleton support. Stable closure is $\mathfrak F_{r,0}(D)=\{D\}$. The bounded base-agreement, positivity, and carrier-bound completion class does not select one such physical $D$. Extending this conclusion to the complete A1--A3 source class is open |
| A7 | Finite order theorem and uniqueness boundary | PROVEN implication: a total monotone deflationary scalar map reaches its greatest fixed point from the top. The all-rung identity, erasure, two-class, and spectator completions have different exact fixed sets inside the bounded completion class. Direct $N$ is not evaluable until the universal source-membership bridge is proved |
| A8 | Extension, refinement, and sewing | PROVEN implications: no-new-confusability injections preserve independent codes; fixed-$D$ refinement eventually stabilizes; exact seam gluing is a fiber product. Unit asymptotic density is insufficient. The physical embeddings, universal source membership, and a sharp finite-size seam or transfer selector are open |
| A9 | Independent geometric estimator | The $\rho_{\rm op}$ discrimination experiment is a commuting-square test of $M_0$ and cannot define it. Protocol non-identifiability and capacity-derived $\rho$ tautology are closed controls |
| A10 | Electroweak/Higgs bridge | Direct record closure plus the screen-load law, exact weak multiplicity four, D10 source relation, and COMMON-EW-LOAD-CARRIER imply $R_{\rm EW}=\alpha_U(P)\log(N/\pi)-6\pi/P=0$ and $N_{\rm bridge}=\pi\exp(6\pi/(P\alpha_U))$. This is downstream of $N$ and does not construct it; the common carrier is open (GAP-A2) |
| A11 | D6 cosmological bridge | Direct record closure plus HORIZON-RECORD-SATURATION implies $\Lambda\ell_\star^2=3\pi/N_{\rm CRC}$. The saturation identification closed not evaluable with the horizon-record exit (GAP-A5) |
| A12 | Identification with the measured α: source transport and endpoint map | OPEN: the V1 grid was exploratory and not target-blind; it used inconsistent P coordinates, mislabeled the required payload coordinate, and supplied no certified interval (GAP-A1) |
| A13 | Identification with the measured Λ and EW coordinate | CONDITIONAL 6.6% CENTRAL-VALUE MISMATCH: the propagated posterior is diagnostic until direct public-record closure, both physical commuting squares, and the frozen likelihood/convention map land (GAP-A5) |

## Part B, step by step

| Step | Content | Status |
|---|---|---|
| B1 | Consensus mechanics and the quantum event surface: quotient normal forms, Born rule, Lüders update, CHSH bound | theorem-grade on the declared algebra-state and two-wing branch; consensus core machine-checked. Consensus selects the commuting public-record algebra, while the quantum representation and joint source state are declared inputs. On the declared binary-icosahedral spinor branch, an exact projective candidate has $\lvert S_{\mathrm{CHSH}}\rvert=1+3/\sqrt5>2$ for one incidence-defined setting family; source-selected settings and a completed two-wing record instrument are absent. The simulator verifies that its declared phase dynamics induces accepted confluent repair on a finite reference packet. Physical phase locking is not a generic premise outside that packet |
| B2 | Kinematics: Conf⁺(S²) ≅ SO⁺(3,1), 3+1 events, H³ observer space | conditional theorem on the typed global-support branch. BW requires `FiniteCapBWCertificate` and independent complete `MGNS-1` on the same tower; the finite GNS certification closed historically under #574. The live physical same-tower continuation is F1 (#694), with structural net inheritance gated by E4 (#701). A structural carrier-to-geodesic-support map is refinement-natural on the declared reference tower, while its physical federation source, nonvacuous higher-overlap coherence, H3/KMS capture, and source-derived common-domain tower are open or quarantined (GAP-B1, LEGACY-GAP-B13) |
| B3 | Gravity: Einstein equation from entropy stationarity, one metric-proportional term per connected component | conditional composition theorem with the antecedent instrumented end to end: every clause carries a fail-closed instrument with adversarial controls and semantic countermodels; coupling universality is theorem-grade for every icosahedrally equivariant law (zero spread) and generator positivity holds by construction on the declared family. The measured convergence ladder ([E1], `evidence/einstein_convergence/`) carries held-out signature (1,3) at every density-maintained rung (16,384/65,536/262,144 carriers) with the cone margin halving per rung and a density control that degrades the signature on cue. Open measured targets with frozen verdicts: cap-state modular temperature (finite no-go at fixed cutoff; refinement-limit form) and the projected cone-margin zero crossing; certified tails, vacuum reference, and independent scale identification stay in the branch premise (GAP-B1, live owner #694) |
| B4 | Gauge structure and matter: the finite A1/A2 Lie-type theorem, conditional matter chain, and independent D7-D9 Tannaka route | Incidence proves the $1+3+3'+5$ decomposition, the unique central graph involution $J$, and $10J=A^3-4A^2-5A+10I$. A target-blind impulse/readback producer derives $R=-J$. Complete compact port response from A1 and endogenous proper-carrier transport from A2 force the abstract type $\mathfrak{su}(3)+\mathfrak{su}(2)+\mathfrak u(1)$; the charged-double-triplet matrices are an exact declared witness, while ordered source tomography and same-current holonomy are open. Inside the declared exterior algebra, the exhaustive 1024-subset anomaly scan selects the unique unordered conjugate nonempty chiral anomaly-free pair, with fermionic grading and finite Spin typing established inside that construction. Anomaly freedom derives determinant balance and tensor-kernel descent computes the common $\mathbb Z_6$ kernel and maximal faithful image on the declared tensors. The cover and its $\mathbb Z_2$, $\mathbb Z_3$, and $\mathbb Z_6$ quotients carry the same local tensors. The six-axis class has order six only after its coefficient relations are declared; complete source characters and a same-source loop-to-kernel theorem are open. Under separate complete-band and cost-order premises, an exact theorem selects the rank-three response band. Tensoring it with the declared fifteen-state table gives a conditional rank-45 candidate; chirality and the diagonal $\mathbb Z_6$ action are properties of that table. A separate 8,662-node receipt checks the declared operator $D_\sigma\otimes I_{45}$ and conditional gap inheritance. The source does not select this matter action, and no certified source, domain, or transport bridge joins the twelve-port Spin packet to the local operator domain. B14 #705 owns finite same-source current and holonomy, B15 #706 finite matter/Spin selection, and B16 #707 primitive period and global form. E4 #701 owns continuum structural-QFT inheritance; H2 #697 owns physical family, pole, and laboratory attachment. |
| B5 | Electroweak chart and hierarchy: forward W/Z chart coordinates, v/E★ transmutation law | W/Z physical comparison not evaluable because the common-observable scheme map is incomplete; hierarchy relation is conditional on its declared branch (GAP-A6) |
| B6 (legacy strong-sector step) | Strong sector: Λ_QCD by transmutation, nucleon mass via external lattice ratio | record at about 1×10⁻²; conditional tags in the closure ledger. This historical label is distinct from Completion Plan V2 B6, the finite holonomy/interference lane. |
| B7 | Cosmology: capacity Λ, dark sector as repair bookkeeping, screen spectrum | exact conditional screen covariance, source-functional amplitude, edge-center tilt, one-shell radial non-identifiability, and conditional physical-dilation/tomography uniqueness; finite source instantiation and physical promotion are work in progress (GAP-B7) |
| B8 | Mass sector completion: flavor spreads, charged leptons, neutrinos | the positive-chamber `C3` circulant identity, conditional finite tracial-GNS balance, `W5` stabilizer bound, scoped 31-axis Cabibbo no-go, 24-register clock no-go, finite modular-gearing compression theorem, and conditional charged determinant-clock implication are closed. The register-Clebsch down-type route is rejected by its protected light-quark ratio. The source-derived family potential, rate/channel realization, physical clock/determinant attachment, chiral-family attachment, phase, numerical flavor ratios, and neutrino sector are open (GAP-B3, GAP-B5, GAP-B4) |
| B9 | Machine formalization | More than 2500 public Lean theorem and lemma declarations, with no `sorry`, `admit`, or project-level `axiom`. Most audited declarations use only Lean's standard extensionality, choice, and quotient axioms. Twenty-three finite proofs and 35 audited downstream declarations also trust `native_decide`'s native-code evaluation axiom. The screen modules include the finite trichotomy, angular branching, four-global-form control, tensor-kernel enumeration, and the explicit $\mathbb Z_6$ intertwiner; analytic physical attachment statements retain their stated executable or paper boundaries (GAP-B8) |
| B10 | Baryogenesis: finite anomaly/current theorem, source attachment, and physical CP-odd generator | anomaly/current theorem and direct gauge/deck no-go done; physical source branch open (GAP-B12) |
| B11 | Primitive twelve-port angular fingerprint | exact on the declared equal-weight cosine branch: for every $0<|ak|\le1$, the full kernel has exactly 62 stationary directions, with 12 maxima, 20 minima, and 30 saddles. The proof combines exact tail bounds, a three-chart 2,624-leaf interval cover, stabilizer-fixed centers, local induced-norm margins, and Morse-signature bounds. Source selection of this spatial operator, coherent frame and boost transport, and physical readout were specified by archived #655; target-clean producer work belongs to G2 (#704), quantitative closure to H1 (#696), and frozen custody to G1 (#695) |

## The open-gap register

This register summarizes the named scientific gaps in the proof package. The
live V2 issue ledger is authoritative for execution ownership; historical
numbers below identify evidence or prior contracts, not active owners unless
explicitly stated.

Part A gaps:

- **GAP-A1 (P-closure resource boundary #545; active owner H1 #696).** The source-emitted Ward-projected
  transport and same-scheme endpoint map. This gap is a frontier of the entire
  field: closure asks for 4×10⁻⁹ relative precision on the hadronic moment,
  beyond every method on Earth, and a source-only payload additionally forbids
  measured hadronic input even for scale setting (see "Why The Hadronic Test
  Is Hard" in [HADRON.md](HADRON.md)).
  Existing grid work is exploratory. The historical v2 contract and V1
  execution cannot discharge the gap; their defect inventory (non-blind
  source, mismatched P coordinates, residual/total confusion, non-certified
  envelope) is recorded on the P-closure issue
  ([#545](https://github.com/FloatingPragma/observer-patch-holography/issues/545)). Corrective v3 is an inactive
  post-target-access erratum scaffold, never an activatable contract.
  Discharged by: issue a detached successor whose complete method is frozen
  before genuinely withheld data or built by an audited clean-room operator;
  emit the source function over the same P coordinate; certify quadrature,
  theory error, and interval bounds; freeze a three-way decision policy; then
  score once.
- **GAP-A2 (electroweak load identification).** The exterior representation
  witness proves four weak-doublet copies per generation. Promoting that
  multiplicity to the physical screen/EW identity requires
  a common electroweak load carrier: construct the $A_5$-invariant public screen-load line,
  the scalar order-unit line of the four-copy weak multiplicity algebra, and a
  refinement-natural positive unital isomorphism intertwining the screen and
  electroweak readouts. One-dimensional positivity and unitality then remove the
  continuous normalization freedom. Equal integer counts do not construct the
  common source-normalized line.
- **GAP-A3 (direct public-record producer and selector): BOUNDED NEGATIVE; COMPLETE-SOURCE BRIDGE OPEN.**
  The canonical capacity surface is owned by the observers synthesis paper.
  The fixed $D=24$ packet contains the frozen carrier, complete terminal
  fiber, atom readouts, endogenous reachability, publicness, joint checkpoint
  kernels, carrier projections, exact graph capacity, scalarization, and
  extension/refinement controls. The capacity-indexed generation-register
  packet has exact all-rung arithmetic and checks its transported controls on
  rungs one through six. The Lean result is parameterized by admissibility.
  Universal membership in the complete A1--A3 source class and the
  executable-to-Lean membership bridge are unproved, so direct $N$ is not
  evaluable. Issue #589 is not evaluable because no source-selected positive
  capacity carrier exists for the horizon identification.
- **GAP-A4 (CP-3): CLOSED, 2026-07-19.**
  Averaging re-emission belongs to fluctuation or dynamics claims. It is neither
  necessary for the direct fixed-point location nor a producer of
  $F_{\rm act}$.
- **GAP-A5 (comparison surface; #589 closed not evaluable, #547 queued).** The nominal 6.6% central-value difference between the
  independent electroweak and $\Lambda$-located comparison coordinates is not
  evaluable as a common-observable residual. Evaluation requires an additional
  source-complete capacity result from GAP-A3, the common electroweak load carrier from GAP-A2, and the
  horizon/record saturation identification:
  $\log M_{0,r}(D_\star)$ must converge to
  $A_r/(4\ell_{\star,r}^2)$ on the same refinement family. It also requires a
  frozen likelihood and convention map. The independent $\rho_{\rm op}$ test
  compares $\log M_0$ with $\pi/\rho_{\rm op}^2$ and cannot define
  either side. Existing posterior propagation remains diagnostic.
- **GAP-A6 (electroweak readout issue #594, generator G3).** The strict-one-loop algebraic kernel is closed:
  a complete declared renormalized packet determines the charged and neutral
  complex-pole coefficients, the neutral one-loop-squared mixing term is excluded
  at this order, and strict coefficients are separated from nonlinear square-root
  coordinates. The fail-closed checker binds the exact subject and rejects
  self-attested evidence. Physical closure requires the OPH-to-FJ parameter
  identity and complete tadpole conversion, target-clean EFT matching, a licensed
  source law/covariance, an independent general-gauge self-energy engine with
  finite-order BRST/ST/Ward/Nielsen receipts, pole isolation and amplitudes, and a
  source clock. The evaluated SMDR fixture is post-exposure regression evidence and
  carries no physical pull.
- **GAP-A7.** Closed 2026-07-17. Uniqueness beyond the declared domain (α⁻¹ outside
  [100, 200]) is discharged by the maximal-domain extension certificate
  (`code/P_derivation/runtime/p_global_uniqueness_extension_certificate_2026-07-17.json`).
  The certificate states the maximal analytic domain of both declared readout maps with
  every window, branch, and positivity condition (P > 0; pixel window
  α_U ∈ [0.02, 0.08]; m_Z window ln(μ_U/m_Z) ∈ [0, 50]; edge-sum convergence t₂, t₃ > 0;
  implicit-root nondegeneracy; nonzero readout). An envelope lemma from the window
  bounds and the m_Z-closure identity certifies 1/α₃(m_Z) ≥ 4 and a positive readout
  floor on the whole domain, and an interval sweep of the full pixel window certifies
  that every window-consistent fixed-point candidate of either map lies inside the
  declared physical interval; the exterior fixed-point set is empty (no nonphysical
  solutions exist). Composed with the 2026-07-14 existence and at-most-one
  certificates, each declared map has exactly one fixed point on its maximal analytic
  domain. Public claims are tied to the declared physical interval, which
  contains the certified fixed point.
- **GAP-A8 (count-density route): CLOSED, 2026-07-19.** A count over states whose membership requires reading the
  supplied capacity is diagonal and circular. After the direct correctable-code
  map exists, the noncircular kernel
  $K_r(D,m)=|\{q:M_0(q)=m\}|$ records the full terminal fiber.
  $K_r(D,D)>0$ proves existence of a self-read terminal branch. A scalar
  deterministic map requires singleton support for every nonempty row. A
  diagonal count-density argmax may rank closed branches and is not a
  producer of the N closure (historical #505; active owner H1 #696).

Part B gaps:

- **GAP-B1.** Einstein-branch nonemptiness: construct one antecedent-only
  common-domain tower whose geometry, modular, event, stress, entropy, and scale
  readouts commute with refinement; certify every asymptotic tail; and supply
  universal coupling, a source-derived vacuum reference, and independent scale
  readouts. If the tower is claimed to descend from the Echosahedral carrier
  federation, it must also carry the source-bound carrier-to-support map in
  LEGACY-GAP-B13. Its BW step must carry `FiniteCapBWCertificate` and independently
  produced complete `MGNS-1` on that same tower. The conditional composition is
  explicit; F1 issue #694 owns the live physical same-tower MGNS premise and
  E4 issue #701 gates structural net inheritance. The compact-gauge
  matter-package nonemptiness problem is separate. Status 2026-07-21: the
  antecedent is instrumented end to end (normalization, GNS/intersections,
  event cone, stress/coupling, each fail-closed with adversarial controls and
  countermodels); universality and positivity are theorem-grade on the
  declared family; the measured convergence ladder shows (1,3) at every
  density-maintained rung with the cone margin halving per rung
  (`evidence/einstein_convergence/`, [E1]). Remaining measured targets are
  recorded historically in #595 and tracked live in #694.
- **GAP-B2.** Quantum pole receipts: the photon, gluon, and graviton zeros are classical
  action statements; the quantum pole constructions are open gates.
- **GAP-B3.** The flavor-orbit selector is a potential-derivation problem.
  In the real traceless-symmetric five-dimensional family representation,
  threefold and fivefold fixed points have a double eigenvalue. The twofold
  fixed locus has dimension three, hence two parameters after scaling, and
  admits simple spectrum. Symmetry alone therefore leaves exactly enough
  freedom to carry two mass ratios. A specific screen-derived invariant
  potential must select a unique simple-spectrum orbit. The proven
  two-modulus non-identifiability and twelve excluded declared-structure
  candidates mark the same boundary.
- **GAP-B4.** The neutrino sector: no mixing matrix or absolute mass row is emitted; the
  weighted-cycle candidate is rejected by NuFIT 6.1 (permanent verdict).
- **GAP-B5.** Charged-lepton source completion. A 24-stage register fixes no
  physical frequency, clock scale, or mass because Hamiltonian rescaling
  preserves the register structure. Source-derived reversible rate ratios can
  conditionally reconstruct dimensionless modular gaps, and the oriented
  register can carry them only when its channel-compression residual vanishes;
  the rates, channel realization, cap-state match, and clock calibration remain
  open source receipts. The common determinant-scale implication is closed only
  conditionally: a source-derived operational clock line $L_{24}$ with
  calibrated gap $\Delta_{24}$ and a natural norm-preserving map
  $L_{24}^{\otimes3}\to\det M_e$ gives
  $(\det M_e)^{1/3}=\Delta_{24}$, up to a separately derived normalization.
  Constructing that clock and attachment is open. The positive-chamber face
  circulant obeys the exact identity
  $Q=1/3+(2/3)(|b|/a)^2$, with $Q=2/3$ exactly at
  $|b|/a=1/\sqrt2$. Equal rank-two event blocks and the finite tracial-GNS
  map supply the balanced modulus conditionally. Physical chiral-family
  attachment, phase, and numerical ratios are open. The MCPR triple is
  target-informed and is a diagnostic coordinate; its
  register count cannot supply the common physical scale.
- **GAP-B6.** The Yang-Mills continuum certificate: the repair-gap identity is
  conditional on a source-defined finite atomic collar table, one uniform
  $L^2$ approximate-tensorization bound, and the declared multiresolution,
  reflection-positivity, transfer/intertwiner, and nontriviality receipts.
  Finite-range mixing and collar-CMI decay alone do not supply the projection
  gap. The bundled exact-rational certificate is a theorem-contract witness.
  The physical compact-gauge transfer receipt is open.
- **GAP-B7.** Cosmology promotion gates: the source-derived applicability rule the
  Cassini exclusion demands, the S8 tension, and physical CMB promotion. The
  screen-spectrum theorem selects the conditional edge-center coordinate
  κ_rep = P/[48(P-φ)]. A finite source must emit the infinitesimal reserve
  generator, collar release law and energy, stress, clock, freezeout, physical
  scale, radial-window, null-space, and forward-residual receipts on one DAG.
- **GAP-B8.** Lean formalization of the physics branches. The finite
  screen-current trichotomy, angular branching table, four-global-form control,
  tensor-kernel enumeration, and $\mathbb Z_6$ intertwiner are machine-checked.
  The exact $\mathbb Q(\sqrt5)$ current and matter recomputations are executable
  certificates. Laboratory attachment, continuum quantum field theory, and
  analytic scaling statements remain outside those finite kernel proofs.
- **GAP-B9.** Gauge-route identity and the remaining physical attachments.
  The finite route has an axiom-forced Lie-type layer and a conditional
  representation layer. The complete-response clause in A1 and endogenous
  proper-carrier transport in A2 force
  $\mathfrak u(1)\oplus\mathfrak{su}(2)\oplus\mathfrak{su}(3)$ from the
  twelve-port module and compact classification. Incidence independently
  determines the unique nonidentity central graph involution $J$ and its
  sector spectrum. The response-admissibility contract restricts inverse-port
  readback to signed central involutive graph automorphisms, hence $\pm J$;
  incidence and readback do not reconstruct a matrix current. The registered
  recurrence and readback generate the exact four-dimensional commutative
  word algebra $\operatorname{span}\{I,A,A^2,A^3\}$; it cannot contain twelve
  independent generators, a nonzero bracket, or a nonidentity proper
  recharting. This bounded result leaves order-sensitive port perturbations
  outside its scope. Twelve diagonal port phases have derived rank zero;
  adjoining the connected adjacency tangent generates $\mathfrak u(12)$ with
  derived rank 143. Conditional on the canonical oriented carrier, the exact
  target-free $A_5$-equivariant alternating-bracket search space over
  $\mathbb Q$ has dimension fourteen and a complete rational Reynolds basis.
  Its complete Jacobi system has quadratic coefficient-row rank 38 and an
  exact 11+27 rowspace decomposition. The rank-27 residual equations apply on
  every derivation-weight flat; the exact three-parameter derivation-only
  family supplies no compactness or source selection. The residual variety,
  compactness, source reconstruction, and same-current holonomy remain
  undischarged. The declared
  port-current fixture constructs
  $\mathfrak u(3)\oplus\mathfrak{so}(3)$ as an exact witness of the forced
  type. Under the declared conjugate matter-projector pair, the matter receipt derives
  determinant balance from anomaly freedom and constructs the spin lift and
  fifteen-state modules. The scalar scan finds the compatible charge pair and
  channels but does not select scalar multiplicity. The descent receipt
  computes the common kernel on every declared tensor, the central descent
  congruence, and conditional character/cocharacter arithmetic inside its
  declared relation and line-lattice system. The resulting
  $\mathbb Z_6$ quotient is the maximal faithful image of that table, not a
  source-selected physical global form. This conditional chain consumes no
  physical matter-content selection rule. On the same declared one-generation
  grammar, the exact dimension-six census admits four independent
  baryon-violating classes after the full exterior-algebra relations. The
  product adjoint excludes the ordinary minimal simple-GUT $X/Y$ channel, but
  the finite gauge structure and diagonal quotient do not prove general
  proton stability or supply a decay coefficient, amplitude, or lifetime.
  D7-D9 independently reconstructs a compact group from the refinement-limit
  transportable-sector category. A source-bound commuting square identifying
  that group with the finite current group has not been constructed: it must
  intertwine both actions on the same current and matter sectors, preserve the
  quotient and normalization, and commute with refinement. Matching Lie types
  is not that map. The remaining physical gates include ordered-response
  reconstruction of the matrix current and same-current holonomy, source
  selection of the matter packet, complete source characters and a
  same-source loop-to-kernel map, laboratory current identification,
  exclusion of extra anomaly-free light sectors, the quotient-visible
  face-phase family attachment with general Yukawa matrices, scalar
  attachment and dynamics, the common screen/electroweak load carrier,
  compact-gauge refinement, and continuum spin/QFT realization. The $24/6=4$
  load count is not the missing load intertwiner.
- **GAP-B10.** Independent reproduction: named external parties re-running the mechanical
  claims from a fresh clone (pillar 5).
- **GAP-B11.** Bare patch-net interface projections do not derive invertible
  unitary rechartings. The regulator gluing proposition
  (`prop:regulatedrealization`) requires explicit quantum regulator gluing data.
  Bare interface projections determine no invertible chart-change map:
  with S_i = {0,1,2,3} and S_j = {0,1} both projecting onto a one-bit interface, no
  bijection S_i → S_j and no *-isomorphism M₄(C) → M₂(C) exists. The quantum
  regulator gluing datum (`def:qregdatum`; finite-dimensional
  algebras, overlap subalgebras, invertible recharting *-isomorphisms, cocycle law) is a
  declared regulator input, the proposition realizes it, and `lem:qregdatum-realized`
  supplies it constructively on the realized echosahedral multiresolution carrier
  (presentation circuits give Ad-unitary rechartings with the strict cocycle law), so
  this discharges the local regulator-gluing obligation on that carrier.
  The carrier-to-support bridge (LEGACY-GAP-B13), the two gauge-route identity
  (GAP-B9), the independent same-tower modular algebra-state package owned by
  F1 issue #694, with structural net inheritance gated by #701, and every
  event, stress, entropy, and scale gate retain their
  stated status.
  Machine-verified evidence bundle: `code/regulator_gluing/` (invertibility and
  triple-overlap composition gates pass on the strict and central witnesses; the
  bare-interface-projection countermodel is rejected with structured reasons;
  artifact `code/regulator_gluing/runs/regulator_gluing_evidence_bundle_current.json`).
- **GAP-B12 ([issue #556](https://github.com/FloatingPragma/observer-patch-holography/issues/556)).** Baryogenesis source completion. The finite quotient theorem fixes
  the mixed anomaly coefficient and oriented repair-current functional, proves
  zero current for a CP-symmetric law, and rules out direct use of the natural
  hypercharge-attached $\mathbb Z_6$ determinant/deck phase through
  $k_R^{YWW}=0$. Discharged by: derive a distinct quotient-visible,
  gauge-singlet, electroweak-anomalous record phase; construct its source-only
  transition generator, physical clock and temperature map, CP-odd initial or
  boundary law, and sign-domain coherence; then evaluate the emitted history
  through sphaleron transport, washout, and freeze-out without using the
  observed baryon abundance as a source input.
- **LEGACY-GAP-B13 (carrier, federation, and support integration).** This is
  the historical carrier/support gap label used by this proof-spine register;
  it is distinct from Completion Plan V2 B13, the finite effect-closure
  boundary. The simulator
  computes a finite reference bridge packet: content-bound interface-algebra
  seam maps, accepted confluent repair for the declared phase dynamics, and a
  refinement-natural carrier-to-geodesic-support map. The current two-carrier
  reference has no seam triangle, so its higher-overlap cocycle condition is
  true vacuously and supplies no nonvacuous triple-overlap witness. A physical
  federation with such a witness, its controlled oriented-$S^2$ limit, and the
  H3/KMS capture are open. The independent same-tower modular algebra-state
  package and the full source-derived common-domain continuum tower remain
  separate.

Cross-cutting:

- **GAP-X1.** Discrete structural selections without a consistency-forcing
  derivation. The conditional SM coefficient bracket and exterior weak-doublet
  multiplicity four are closed mathematical results. The generation count
  $N_g=3$ follows from separate complete-band and cost-order premises inside
  the conditional window $3 \le N_g \le 5$. Its tensor product with the
  declared generation table is a conditional rank-45 candidate. The separate
  local operator receipt does not source-select that matter action or bridge
  the twelve-port Spin packet to its 8,662-node domain. Physical matter-pole
  and continuum attachment require separate derivations. The
  laboratory current identification, cross-route group identity, extra-light
  sector exclusion, and load identification remain separate. The abstract
  Lie type does not depend on those selections. The source current, physical
  matter action, and global quotient retain their separate gates.
- **GAP-X2.** CI regeneration of the ledger from the claim registry
  (SLP-02, SLP-05), and builder-script patches so generated surfaces keep their
  supersession notes.

## Reading rule

The spine may only strengthen by dated artifact: a step moves to PROVEN when its
certificate or theorem lands, a gap leaves the register only with a verdict, and no
wording change ever substitutes for either. The specialist derivations present this spine at
full strength; the closure ledger prices it; a corrected target-blind contract and
its certified execution decide the endpoint lane.
