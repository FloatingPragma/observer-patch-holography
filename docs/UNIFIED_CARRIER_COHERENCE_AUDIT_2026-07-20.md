# Unified Carrier Coherence Audit (2026-07-20)

## Scope

This audit fixes the shared ontology and dependency order for the screen
microphysics, consensus, compact Standard Model and relativity papers, and the
corresponding book chapters. OPH is presentation-invariant and
carrier-architecture-sensitive. A relabeling is silent when it preserves the
complete observer-facing signature. Visible port incidence, response maps,
repair laws, records, clock data, and refinement maps can constrain the
admitted physics.

Simulator runs have no theorem or empirical-promotion role in this audit. The
status statements below rest on analytic premises, proofs, countermodels, and
source-bound certificate contracts.

## Three typed screens

The word *screen* denotes three distinct objects.

1. A local Echosahedral carrier has a twelve-port boundary with oriented
   incidence.
2. A federation screen consists of routed carriers, operational observer
   supports, accepted repairs, and public records.
3. A global support screen belongs to the geometry branch and has an oriented
   (S^2) refinement limit under its producer receipts.

A carrier is neither an operational observer nor a support-chart cell by
definition. Operational observer status requires local state, visible
interfaces, readback, durable records, feedback or repair, prediction or
control, and checkpoint continuation. A source-bound, refinement-natural map
from the carrier federation to the global support complex is open. Local
(A_5) incidence alone does not determine the federation nerve, global
topology, conformal support, or Bisognano-Wichmann flow.

## Shared dependency order

```text
local twelve-port carrier boundary
  -> operational observer supports and routed federation screen
  -> accepted quotient repair and public normal form
  -> source-bound carrier-to-support map
  -> global S2 support and finite cap-flow certificate
  +  independent complete algebra-state modular certificate on the same tower
  -> support-visible Bisognano-Wichmann identification
  -> Lorentz response, event manifold, and conditional Einstein branch

local A5 incidence
  -> source-produced compact current
  -> conditional Standard Model Lie-type recognition
  -> global form, matter selection, family attachment, and QFT producer gates
```

Phase locking is a candidate synchronization mechanism. Consensus requires
accepted descent, local diamond resolution, any declared fairness condition,
and equality of quotient normal forms. The phase-to-consensus bridge is open.

## Gravity and clock boundary

The finite cap-flow certificate contains support geometry, cap flow,
orientation, group law, continuity, and normalization. A separate complete
algebra-state modular certificate supplies conditional expectations,
comparison maps, cyclic and separating vectors, modular covariance, and the
cofinal convergence data. The Bisognano-Wichmann theorem consumes both inputs
on one refinement tower.

Modular flow supplies dimensionless local ordering. Physical time requires an
observer-readable transition, correspondence with event classes, affine
calibration, gluing across overlaps, and cycle consistency. Event charts have
rank four under the population, separation, and open-image receipts. Lorentz
signature additionally requires the held-out cone, causal reachability, and
assembly receipts. Hyperbolic three-space is the observer-frame fiber over the
event base.

## Gauge-route boundary

The finite (A_5) current route and the Tannaka/MAR route are independent
conditional constructions. Matching Lie type does not identify their physical
groups. Identification requires a source-bound commuting square with a
refinement-natural group isomorphism, action intertwiners on the same current
and matter sectors, and agreement of the quotient, Spin/deck data,
normalization, and character lattices.

Minimal Admissible Realization (MAR) selects (N_g=3) as the least value in
its declared economy class. Physical three-family content requires the complex
rank-45 attachment, locality, residue, symmetry-breaking or descent, and
refinement receipts. The icosahedral rank-three band is a candidate family
carrier with no generation-count theorem status.

## Theorem status

| Interface | Exact status |
| --- | --- |
| Twelve-port selector | Proved on the declared Echosahedral carrier lineage |
| Carrier to operational observer | Definition and receipt contract; physical realization is open |
| Phase lock to consensus | Candidate mechanism; accepted-repair and confluence bridge is open |
| Carrier federation to global (S^2) support | Separation no-go proved; positive source-bound map is open |
| Finite cap geometry to BW | Requires the finite cap-flow certificate and an independent complete same-tower modular certificate |
| Event manifold | Conditional on the full population, chart, cone, causal, and assembly receipts |
| (A_5) current to Standard Model Lie type | Exact conditional recognition theorem |
| (A_5) current group to Tannaka/MAR group | Source-bound commuting square is open |
| Three generations | MAR economy selection; physical rank-45 attachment is open |
| Einstein and QFT branches | Conditional compositions; inhabited source towers and physical producers are work in progress |

Paper release identifier: `r1564`.
