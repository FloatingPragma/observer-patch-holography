# OPH Selection Ledger

Generated by `tools/build_selection_ledger.py` from `claims/selection_ledger.json`; edit the JSON, then regenerate.

OPH builds public reality from finite observers that read patches of a shared
screen, compare overlaps, and repair disagreement until every observer agrees.
On the declared carrier lineage that consensus requirement forces the twelve-port
screen geometry outright, with machine-checked proofs, and every remaining
structural choice is finite: a declared premise with a stated menu, or an open
item with a named owner. There is no tunable layer anywhere in the list. This
ledger records every discrete structural selection in the framework exactly
once, classified as FORCED (derived from the axioms and source objects), EXPOSED
PREMISE (chosen and declared in theorem antecedents), or OPEN (neither, with the
owning GitHub issue). It shows exactly what is proved and exactly what is left:
pick an open row and work on it. It is the canonical free-versus-forced surface
for
[issue #554](https://github.com/FloatingPragma/observer-patch-holography/issues/554).

## Forced by consensus

| # | Selection | Where it is forced | Menu beaten |
| --- | --- | --- | --- |
| 1 | Twelve-unit port splitting (all twelve port weights equal one) | Issue #565 closure packet (`code/a5_closure`): unique all-one total-12 allocation with exact floor 12, next floor 14, gap 2. Lean: `Lean/Screen/UnitSplit12.lean`, `unit_split_of_positive_sum`. Conditional on row 4. | 1352078 (integer twelve-defect splittings that minimize a linear cost; 1,352,078 compositions survive a linear cost and the strictly superadditive cost selects the all-unit splitting uniquely) |
| 2 | Inverse port pairing and six axes | Issue #565 closure packet: unique distance-3 antipodal pairing and six axes on the port incidence, with negative controls. Conditional on row 4. | 10395 (fixed-point-free pairings of twelve labels; 10,395 pairings exist without incidence and oriented incidence at distance three selects the antipodal pairing uniquely) |
| 3 | Icosahedral screen selector and port frame (A5 action, rank-3 Gram frame) | Issue #565 closure packet: Aut+ isomorphic to A5, full Aut isomorphic to A5 x C2, Gram identity G^2 = 4G with trace 12. Lean: `Lean/Screen/PortFrameGram.lean`, `Lean/Screen/A5PortAction.lean` (kernel `decide`). Conditional on row 4. | 2 (oriented A5 frame or orientation-forgetting A5 x C2 frame; the orientation receipt fixes the oriented class) |
| 12 | Physical port-current realization | The semantic response artifact measured from the carrier dynamics determines the charged response representation, its four signed coefficients, the oriented frame, and the physical refinement maps; the port-current certificate recomputes every exact artifact claim in Q(sqrt5) and its physical source gate passes (issue #599 closure packet). Lean `Lean/Screen/PhysicalA5ForcingNoGo.lean` (`noSourceOnlyCurrentReconstruction`) records why the artifact is necessary: the bare source reduct admits two inequivalent completions. Conditional on row 4. | 2 (two inequivalent current completions of the same source reduct; a bound semantic response artifact determines one) |
| 14 | Physical matter spin lift | The matter-lift certificate over the source-bound current packet forces the non-split binary-icosahedral spin lift (unique involution, split lifts impossible), derives the trace-balanced charge pair by BLOCK-DETERMINANT-BALANCE, derives the scalar and channel selection, and intertwines the artifact's physical refinement maps on the carrier and the Fock realization; its physical source gate passes (issue #314 closure packet). Conditional on rows 4 and 12. | 2 (split SO(3) port lift or non-split binary icosahedral lift; the spin-lift certificate forces the non-split cover) |

## Exposed premises

| # | Selection | Where it is declared | Menu |
| --- | --- | --- | --- |
| 4 | Echosahedral carrier lineage (icosahedral geometry for the screen carrier) | Declared quotient-visible carrier lineage of the #565 selector; the packet does not derive that every OPH carrier is echosahedral. Lean `Lean/Screen/PhysicalA5ForcingNoGo.lean` (`noSourceOnlyChargeReconstruction`) proves the bare total-charge reduct cannot force the choice, so a declared premise is the exact boundary. | 3 (tetrahedral, octahedral, icosahedral antipodal port families) |
| 5 | Compact-gauge refinement receipt | Declared in the D7 antecedent of the compact paper and the synthesis paper: cofinal tail with coherent surjective charge pullbacks, finite block embeddings, tensor realizations, 3+1D symmetry, and compatible forgetful fibers. | declared receipt object; no finite alternative enumeration exists, so the row is a theorem antecedent and not a compression-menu input |
| 6 | MAR matter class with the one-generation/one-Higgs witness (N_g = 3 as least economy value) | Minimal admissible realization is axiom five; the low-energy packet with the one-generation/one-Higgs chiral witness is declared in the D8/D9 antecedents. The packet admits 3 <= N_g <= 5 and selects N_g = 3 as the least value of the declared economy class. | 3 (N_g in {3, 4, 5} within the declared economy class) |
| 7 | Z6 reserve pricing input l_shared = P/4 | Declared branch input to the reserve-trace theorem (`thm:z6-reserve-trace`, screen microphysics paper); every downstream reserve number inherits it. | declared continuous input; no finite alternative enumeration exists, so the row is a theorem antecedent and not a compression-menu input |
| 8 | BR-0: cell product reading of the screen count | `code/capacity_readback/F_CONSTRUCTION_2026-07-14.md`, Step 2: reading (i) is the only executable reading and is carried with its warning as an inherited hypothesis; it reproduces the declared screen dimension exactly. | 2 (reading (i) treats the cells as independent record carriers; reading (ii) refuses the product structure and derives no count) |
| 9 | Publicness policy | Frozen publicness policy declared in the finite closure construction of the synthesis paper; deriving it from source objects belongs to the issue #505 program. | frozen policy object; no finite alternative enumeration exists, so the row is a theorem antecedent and not a compression-menu input |
| 10 | Scheduler selection (fair-block contraction branch) | Declared in the consensus-paper fair-block antecedent, where the implementation supplies the contraction data for the statement. | declared branch with continuous contraction data; no finite alternative enumeration exists, so the row is a theorem antecedent and not a compression-menu input |
| 11 | Quantitative closure map declaration (pixel closure) | Each candidate closure map is declared before evaluation; the interval certificate proves exactly one fixed point per declared map on the physical domain (compact proof, certificate C1). The physical Thomson identification is a separate receipt. | per declared map; the map space has no finite alternative enumeration, so the row is a theorem antecedent and not a compression-menu input |

## Open: the work-wanted list

| # | Selection | Owner and state | Menu state |
| --- | --- | --- | --- |
| 13 | Determinant, spin, loop, axis, and refinement descent to the physical Z6 global form | Abstract six-axis quotient arithmetic is proved in Lean `Z6Exact.lean` and the tensor-spin kernel in `TraceBalancedKernel.lean`; the physical cocharacter-lattice descent, the exact line spectrum, and the four-global-forms negative control are the target of issue #567. Owner: [#567](https://github.com/FloatingPragma/observer-patch-holography/issues/567). | the admissible global-form menu awaits the four-global-forms negative control of issue #567 |
| 15 | Screen-to-family attachment (rank-45 map to three rank-15 families) | The canonical lowest rank-three screen band exists; its source-derived complex-linear attachment to the physical matter-pole residue space is the target of issue #569. The exact 31-axis spectrum excludes only direct real-3D residual-axis equality with the Cabibbo angle. Owner: [#569](https://github.com/FloatingPragma/observer-patch-holography/issues/569). | the attachment menu is not enumerated; spinorial, broken-symmetry, and general overlap routes are open |
| 16 | Capacity-closure selector (exact finite-size slack law with one physical zero) | Source-only correctable public-record closure and the unique slack zero are the targets of issues #505 and #551. Owner: [#505](https://github.com/FloatingPragma/observer-patch-holography/issues/505), [#551](https://github.com/FloatingPragma/observer-patch-holography/issues/551). | the closure-selector menu is not enumerated before the slack law exists |
| 17 | BR-1: reserve semantics | Declared menu in `F_CONSTRUCTION_2026-07-14.md`, Step 3; the branch axis is selected by the slack law of row 16. Owner: issue #551. Owner: [#551](https://github.com/FloatingPragma/observer-patch-holography/issues/551). | 3 (poisson survival e^(-P/24), presence survival 1 - P/24, or no exclusion) |
| 18 | BR-2: reserve attachment | Declared menu in `F_CONSTRUCTION_2026-07-14.md`, Step 3; the branch axis is selected by the slack law of row 16. Owner: issue #551. Owner: [#551](https://github.com/FloatingPragma/observer-patch-holography/issues/551). | 2 (one reserve unit per equal-area cell, or reserve priced on the 3K shared edges) |
| 19 | BR-3: count effect of the readback record | Declared menu in `F_CONSTRUCTION_2026-07-14.md`, Step 4; the branch axis is selected by the slack law of row 16. Owner: issue #551. Owner: [#551](https://github.com/FloatingPragma/observer-patch-holography/issues/551). | 4 (port multiplicity X+, delta constraint X0, record cost X-, write plus check cost X=) |
| 20 | BR-4: observer/checkpoint subfederation marking | Declared menu in `F_CONSTRUCTION_2026-07-14.md`, Step 5; the branch axis is selected by the slack law of row 16. Owner: issue #551. Owner: [#551](https://github.com/FloatingPragma/observer-patch-holography/issues/551). | 3 (unmarked K0, one marked chain K1, chain plus checkpoint K2) |
| 21 | BR-5: symmetry quotient | Declared menu in `F_CONSTRUCTION_2026-07-14.md`, Step 6; the branch axis is selected by the slack law of row 16. Owner: issue #551. Owner: [#551](https://github.com/FloatingPragma/observer-patch-holography/issues/551). | 3 (A5 quotient S-, quotient representatives S0, flag multiplicity S+) |
| 22 | BR-6: Cap_read family | Declared menu in `F_CONSTRUCTION_2026-07-14.md`, Step 7; the branch axis is selected by the slack law of row 16. Owner: issue #551. Owner: [#551](https://github.com/FloatingPragma/observer-patch-holography/issues/551). | 4 (CAP-L log-count readback and three declared alternative Cap_read families; CAP-B is barred before evaluation) |

Totals: 22 selections. 5 FORCED, 8 EXPOSED PREMISE, 9 OPEN.
Every open row belongs to the particle-realization and capacity programs;
the recovery of relativity and quantum structure from observer consensus
consumes only the forced rows and declared premises above.

## How to read a row

A FORCED row is a theorem: given the axioms and the cited premises, the
selection has no alternative, the citation names the machine-checked or
certified proof, and the menu column records the enumerated alternative
set the theorem excluded. An EXPOSED PREMISE row is a declared choice: the
theorem downstream of it states the choice in its antecedent, and the menu
column records how many alternatives the declaration passed over. An OPEN
row is a selection that is neither derived nor stated as a clean
antecedent; the owning issue tracks the work that moves it into one of the
other two classes. A row can move up (OPEN to EXPOSED PREMISE to FORCED)
and never silently disappears: the consensus argument is exactly as strong
as the FORCED column plus the declared premises it names. Rows with a
numeric menu feed the compression accounting in
`claims/physical_identification_registry.json`; rows whose declared object
has no finite alternative enumeration carry their antecedent location
instead and are excluded from compression accounting by construction.
Every open row is a well-posed problem with an issue link; solving one
moves it up the ledger.
