# V3 Emergent-Instrument Register

Generated by `tools/build_instrument_register.py` from `claims/emergent_instrument_register.json`; edit the JSON, then regenerate.

One row per simulation-instrument design or frozen instrument of the registered adequacy program. Lane [#737](https://github.com/FloatingPragma/observer-patch-holography/issues/737) owns the instruments and this register. Each instrument binds to exactly one row of the observation ledger (`docs/OBSERVATION_LEDGER_V3.md`). SPECIFIED is mutable design work, not a preregistration or verdict. Only a completed decisive instrument explicitly selected by the ledger-control lineage can apply its declared consequence to the bound ledger row.

## Separation from the frozen-prediction ladder

The frozen-prediction ladder (`docs/FROZEN_PREDICTION_LADDER.md`) is reserved for physical predictions against external data. Simulation instruments live on this register and never enter that ladder: an instrument verdict concerns what simulated observers' records exhibit inside the architecture and makes no physical prediction.

Freeze artifacts pin `{path, sha256}` pairs. Paths under `oph-physics-sim/` require a canonical repository URL and full commit pin. When the sibling simulator checkout is present, the validator resolves that exact commit and hashes the Git object bytes for every listed artifact. Every other relative path must exist in this repository and match its pinned digest.

## Policy

This register distinguishes an open SPECIFIED design from a preregistered FROZEN instrument: SPECIFIED means mutable design only, with no freeze, authorization, or evidential verdict. A completed positive verdict can support an observation-ledger rung only when the committed receipts satisfy the frozen decision rule and the ledger-control lineage explicitly selects it. Each ledger control declares the exact row-status consequence of a selected REPLICATED or FAILED verdict and the open premises that verdict cannot remove; a later design or incomplete run cannot overwrite either boundary. A ledger row with no decisive completed instrument carries an explicit null controller and cannot be marked attained; a later decisive verdict must be selected explicitly in the same scientific update that applies its declared ledger consequence. The register is separate from the frozen-prediction ladder, which is reserved for physical predictions against external data. Negative and control outcomes are reported with the same prominence as positive ones. Fail closed on post-hoc analyses labeled as validation, parameters revised after data, silent reruns, omitted controls, and any run whose configuration is not reproducible from committed artifacts.

## Status enum

- **SPECIFIED**: A design specification is recorded and no freeze has happened. The row carries no freeze artifacts, no freeze time, and no verdict receipts; the decision rule may be a pointer into the specification.
- **FROZEN**: The decision rule, seeds policy, and controls are pinned before any run. The row carries the verbatim decision rule, a non-future UTC freeze time, and hashed freeze artifacts; no verdict receipt exists.
- **RUNNING**: The frozen instrument executes under its pinned configuration. The row carries its freeze fields and no verdict receipts.
- **REPLICATED**: The frozen decision rule returned its positive verdict. The row carries freeze artifacts and verdict receipts, but it supports a ledger rung only when the ledger-control lineage explicitly selects it.
- **FAILED**: The frozen decision rule returned its negative verdict. The row carries freeze artifacts and verdict receipts. When controlling, it applies the ledger control's declared negative consequence with equal prominence.
- **INCONCLUSIVE**: The frozen decision rule returned no verdict. The row carries freeze artifacts and verdict receipts, and the bound ledger row is neither promoted nor demoted by this instrument.
- **VOID**: The registration is withdrawn or superseded. A VOID row issues no verdict, and it carries freeze artifacts exactly when it carries a freeze time.

## Coherence rules (validator, fail-closed)

A SPECIFIED row carries no freeze time, no freeze artifacts, and no verdict receipts. A FROZEN or RUNNING row carries a non-future UTC freeze time, at least one hashed freeze artifact, a decision rule naming REPLICATED, FAILED, and INCONCLUSIVE, and no verdict receipts. A REPLICATED, FAILED, or INCONCLUSIVE row additionally carries at least one verdict receipt. A VOID row issues no verdict and carries freeze artifacts exactly when it carries a freeze time. A REPLICATED instrument supports its observation row only when it is explicitly selected by the ledger-control lineage. Each control declares the exact row status required by REPLICATED and FAILED and the open premises that each verdict must preserve. A represented ledger row with no decisive completed instrument has a null controller and cannot be attained. A later decisive verdict must be explicitly selected before its declared consequence applies. An artifact entry whose path resolves inside this repository is hashed against its pinned digest. External entries require a canonical repository and full commit pin; their Git object bytes are verified whenever the sibling custody checkout exists.

## Ledger-control lineages

- OL-A1: controlling instrument `INS-01`. REPLICATED consequence `attain_row` preserving open premises none; FAILED consequence `owe_row` preserving open premises none. INS-01 remains controlling after its FAILED verdict. A later instrument may supersede it only when that instrument is REPLICATED under its frozen decision rule and is then explicitly selected here. Until both conditions hold, OL-A1 remains owed.
- OL-C5: no completed controlling verdict. REPLICATED consequence `support_only` preserving open premises PR-03; FAILED consequence `block_attainment` preserving open premises PR-03. No completed decisive instrument controls OL-C5. INS-03 is SPECIFIED design work only and cannot move the ledger row. A later decisive verdict must be selected explicitly here in the same scientific update that applies its ledger consequence.

## Instruments

| Row | Instrument | Ledger row | Lane | Status | Frozen (UTC) |
| --- | --- | --- | --- | --- | --- |
| INS-01 | OL-A1 (1,3) signature replication instrument | OL-A1 | [#737](https://github.com/FloatingPragma/observer-patch-holography/issues/737) | `FAILED` | 2026-08-12T19:14:51Z |
| INS-02 | OL-A1 factorial carrier-count and support-size follow-up | OL-A1 | [#737](https://github.com/FloatingPragma/observer-patch-holography/issues/737) | `SPECIFIED` | unfrozen |
| INS-03 | OL-C5 source-bound phase-sensitive readout instrument | OL-C5 | [#737](https://github.com/FloatingPragma/observer-patch-holography/issues/737) | `SPECIFIED` | unfrozen |

### INS-01 OL-A1 (1,3) signature replication instrument

- Ledger row OL-A1; owning lane [#737](https://github.com/FloatingPragma/observer-patch-holography/issues/737); status `FAILED`.
- Specification: `plan/OL_A1_SIGNATURE_REPLICATION_SPEC.md (oph-meta planning workspace)`.
- Custody repository: `https://github.com/muellerberndt/oph-physics-sim` at commit `42aa96607f40dae94c5a8b65e9dd8e71e5b6434e`.
- Decision rule: Pointer while SPECIFIED: the binding text is the Tier A decision rule in the specification, copied verbatim into this field at freeze time. Tier A summary: arms A1 = 16384/128/96, A2 = 65536/256/96, and C1 = 16384/128/6 with five declared replicates per arm; observables O1 threshold inertia, O2 robust inertia at relative tau 1e-3 with reference (1,2)/(1,2)/(2,1), O3 degeneracy ratio against the reference band 2e-5..4e-5, O4 cone margin and rung ratio (retained 0.5725, declared band 0.35..0.80), O5 structural diagnostics, and O6 split-half inertia concordance; verdict REPLICATED, FAILED, or INCONCLUSIVE per the frozen rule, and FAILED demotes the OL-A1 emergent rung with equal prominence. Eigenvalue caution: the archived (1,3) threshold verdict rides on one eigenvalue at relative magnitude about 3e-5 against a 1e-12 threshold; at a relative threshold of 1e-3 the robust inertia of the retained rungs is (1,2) with one degenerate direction, matching the independently measured (1,2) receipt, and a fresh seed can flip (1,3) to (2,2) with no change in physical content. Every public sentence about this instrument carries that caution.
- Seeds policy: One fresh seed drawn at freeze time and five declared replicate ids per arm. The held-out split is stride-structured per rung (strides 8/35/141), so train/held-out parity structure is recorded and stratified across replicates. N = 5 resolves seed fragility at the 1-in-5 level only, and the report states so. The runtime environment is single and recorded.
- Lineage predecessor: none. This does not change the controlling verdict.
- Reproducibility boundary: The committed campaign retains the manifest, summary, and fifteen per-arm receipt files but no raw feature matrices or fit captures from which an independent implementation could reconstruct every observable. The simulator's validate-only mode rechecks the committed receipt fields and frozen decision rule using the producer implementation; it is not an independent observable recomputation. The pinned campaign commit is present in the sibling custody checkout and is reachable from the configured GitHub remote's main branch; remote reachability closes only the publication-custody check and does not supply the missing raw captures or an independent observable recomputation. Instrument defect found 2026-08-21: the event chart mixes an integer ancestry-depth coordinate with three spectral coordinates whose spreads differ by about three orders of magnitude (depth standard deviation 29.11 against spectral 0.043 to 0.049 on a 2,048-carrier capture), so the quadratic form was dominated by the depth direction and the spectral directions collapsed to an eigenvalue at 1.3e-05 relative magnitude; the reported signature turned on which side of the inertia cut that degenerate direction fell, which is the recorded seed fragility. Repaired in oph-physics-sim commit 017ec2a by standardize_chart and a standardize flag on the fit, which resolves every direction and makes the signature invariant under per-coordinate rescaling, with the flag defaulting to off so committed receipts replay unchanged. The repair does not restore discriminating power: against the C-ANCESTRY null the null still reproduces the data inertia in 4 of 5 permutations before and after, and the held-out cone margin is unchanged and still negative, because eigenvalue sign counts do not test attribution to ancestry structure. No repaired rerun was performed on that basis. The measurement was removed from the reader-facing papers on 2026-08-21 and is retained here as the instrument record.
- Controls:
  - C-SUPPORT: matched support-density control at 16384/128/6; a fired control reads as mean signature degradation plus halved cross edges.
  - C-ANCESTRY: ancestry-permutation null at zero extra runtime; the null destroys (1,3) on at least 4 of 5 replicates.
- Freeze artifacts:
  - `oph-physics-sim/scripts/ol_a1_signature_replication.py` sha256 `6cd6b179e9f62e7d7f5f1177ec4cfa329a07c0279ef9e7aedc7aef2383e8acc1`
  - `oph-physics-sim/docs/OL_A1_PREREGISTERED_SIGNATURE_REPLICATION_2026-08-12.md` sha256 `b4953ec083e9af7121baf0656d97ab538598e705575f7b6742a26c9ade7490ae`
  - `oph-physics-sim/docs/OL_A1_SEED_TABLE_2026-08-12.json` sha256 `acc8d5d67c2a4eaffe25c07fcdbfdcd7175a8b9a82142b8edb53d0d402f70f05`
- Frozen (UTC): 2026-08-12T19:14:51Z.
- Verdict receipts:
  - `oph-physics-sim/data/ol_a1_replication/manifest.json` sha256 `befad341fdc5dfe8dcc1370c9ec613748b3891f7744957b133251ac36ac85d28`
  - `oph-physics-sim/data/ol_a1_replication/campaign_summary.json` sha256 `e9f8cd54f1451b546a88341f678203f87f500dd4207584309dbe9a0612d2fe6b`
  - `oph-physics-sim/data/ol_a1_replication/run_A1_ola1.r1.json` sha256 `66be4d632ae4a37b42369b6a89ee0bee4d6c667fd9cc612449fbc7f7fbb9b515`
  - `oph-physics-sim/data/ol_a1_replication/run_A1_ola1.r2.json` sha256 `87841e0a5fab7feca514cba821c158c177bb308c53cba6f9d3a8c96d2276f765`
  - `oph-physics-sim/data/ol_a1_replication/run_A1_ola1.r3.json` sha256 `03d5b2e305bd5652d73b60bdc757d8c9812007a9b81648602e02772522161674`
  - `oph-physics-sim/data/ol_a1_replication/run_A1_ola1.r4.json` sha256 `07c43363eb37ae908d6ef2d203b9b4afbe080d05a52802c69ce20d80bc649ece`
  - `oph-physics-sim/data/ol_a1_replication/run_A1_ola1.r5.json` sha256 `bf81a4f85aca32f6292458005d481590d7c9556ae9829025a607d9c0f048125a`
  - `oph-physics-sim/data/ol_a1_replication/run_A2_ola1.r1.json` sha256 `0a4dd46e524914f54c307ebbd970e85fe8bba1abc733f2e62b6a970c32b1e016`
  - `oph-physics-sim/data/ol_a1_replication/run_A2_ola1.r2.json` sha256 `e2da7a503978ef7e66b780a40017e491c09f80e03da10c9c5f0095975f64a416`
  - `oph-physics-sim/data/ol_a1_replication/run_A2_ola1.r3.json` sha256 `86f387b87ae7dd16792a0df6a1f5d09b99c759deb30f8696848739b1fdd67d24`
  - `oph-physics-sim/data/ol_a1_replication/run_A2_ola1.r4.json` sha256 `83e1719b8efec4a73a9decf9f2cb8ddddb46ed1f4fb5d842ee7a04771c1e5bba`
  - `oph-physics-sim/data/ol_a1_replication/run_A2_ola1.r5.json` sha256 `5dc398c3f86f8b172bfd5cee1332c3077d7c69a7381f4b46092fc96953e77eb4`
  - `oph-physics-sim/data/ol_a1_replication/run_C1_ola1.r1.json` sha256 `ae4d18d3c7a4c113d5fa279b4198218589d546b962b7294736f48045c8b0b8c4`
  - `oph-physics-sim/data/ol_a1_replication/run_C1_ola1.r2.json` sha256 `b38f02987fb5c10b8ef4971b6078b4cda2943f2f0258fd3267416618cc2abcb2`
  - `oph-physics-sim/data/ol_a1_replication/run_C1_ola1.r3.json` sha256 `39cbb3c7a4bcd37c71e68302bece6dba606bbe7f3f3413d077a6c5257e0b1bd7`
  - `oph-physics-sim/data/ol_a1_replication/run_C1_ola1.r4.json` sha256 `109257fa22802d15c9fd8d2bff14dfc0e1584d634e05bda038c77a559d387e7c`
  - `oph-physics-sim/data/ol_a1_replication/run_C1_ola1.r5.json` sha256 `0461bc690c019ed34ad974aee7deaba0247c2d64b607c78ba107202e649404f4`

### INS-02 OL-A1 factorial carrier-count and support-size follow-up

- Ledger row OL-A1; owning lane [#737](https://github.com/FloatingPragma/observer-patch-holography/issues/737); status `SPECIFIED`.
- Specification: `plan/OL_A1_FACTORIAL_FOLLOWUP_DESIGN.md (oph-meta planning workspace)`.
- Custody repository: none (unfrozen; no artifacts pinned).
- Decision rule: Design-stage pointer only. The mutable design envelope crosses candidate carrier counts 16,384; 65,536; 131,072; and 262,144 with candidate absolute support sizes 48; 96; and 192 at one observer count fixed at the future preregistration, provisionally 256. A later freeze must report threshold stability, continuous spectral margins, carrier-count and absolute-support-size main effects, their interaction, quantitative ancestry-destruction and synthetic-sensitivity gates, sham equivalence, and cone-margin scaling separately through one fixed estimator before applying a predeclared overall REPLICATED, FAILED, or INCONCLUSIVE rule. No component pass may overwrite a failed control or endpoint, and no component failure may erase a separately reported component pass. These values and rules remain mutable until a target-blind freeze and do not authorize execution.
- Seeds policy: No seed has been drawn. Before a future freeze, a target-blind prospective precision and power calculation must determine the replicate count from declared minimum effects, interval widths, multiplicity, and attrition; no fixed count is presumed sufficient. Fresh seeds form paired blocks spanning every retained factorial cell and control, with cell execution order randomized within each block, no seed redraw or optional stopping, and one recorded runtime environment. Runtime-only pilots may measure cost but may not expose signature, margin, or control outcomes.
- Lineage predecessor: INS-01. This does not change the controlling verdict.
- Reproducibility boundary: This is an open SPECIFIED design, not a preregistration or authorization to run. Candidate carrier counts, absolute support sizes, fixed observer count, replicate count, statistics, and quantitative gates remain mutable until a target-blind freeze. INS-01 remains the controlling completed verdict and OL-A1 remains owed; INS-02 cannot supersede it unless a later frozen run returns REPLICATED and the ledger control explicitly selects it.
- Controls:
  - Degree-preserving ancestry rewiring must preserve carrier count, degree sequence, support counts, and one-point record marginals while breaking lineage correlations.
  - Depth-stratified lineage shuffling must preserve the population at every declared depth while breaking cross-depth family identity.
  - A sham identifier relabeling must preserve the complete ancestry graph and leave the analysis invariant.
  - A synthetic ancestry-sensitive statistic of known sign must pass a predeclared quantitative recovery gate through the same estimator used for the main and ancestry-destroyed cells; it has no promotion authority.
- Freeze artifacts: none (unfrozen row).
- Frozen (UTC): none.
- Verdict receipts: none (no run verdict).

### INS-03 OL-C5 source-bound phase-sensitive readout instrument

- Ledger row OL-C5; owning lane [#737](https://github.com/FloatingPragma/observer-patch-holography/issues/737); status `SPECIFIED`.
- Specification: `plan/INS03_SOURCE_BOUND_PHASE_INSTRUMENT_DESIGN.md (oph-meta planning workspace)`.
- Custody repository: none (unfrozen; no artifacts pinned).
- Decision rule: Design-stage pointer only. A future target-blind freeze evaluates, in order, the source-produced common-preparation, provenance, and phase-discrimination gates; the PR-64 outcome-map, trace, effect, and readback interface; the controls; the phase residual and PR-03 additivity residual; and finally a non-promoting declared-table conformance check. PASS maps to REPLICATED, FAIL maps to FAILED, and SOURCE_PRODUCER_MISSING or INCONCLUSIVE maps to INCONCLUSIVE while retaining the specific receipt word. PASS requires a source-produced non-record-diagonal preparation that clears a frozen phase-content threshold, a phase residual inside EPS_PHASE, an additivity residual inside EPS_ADD with enough resolution to reject the committed -3/256 countermodel, exact algebraic interface residuals, finite-sample readback residuals within TOL_READBACK, and every control. No numeric placeholder is bound here, no component pass overwrites a failed control or endpoint, and this mutable rule authorizes no execution.
- Seeds policy: No seed has been drawn. Before a future freeze, every threshold, interval construction, multiplicity family, context and effect list, excluded-input set, missing-data rule, and block count binds target-blind. A prospective precision calculation fixes the block count from declared effects, interval widths, multiplicity, and attrition. One fresh master seed then uses a frozen collision-free derivation to paired block and cell seeds, with execution order randomized within blocks, no redraw, no substitution, no optional stopping, and one recorded runtime environment. Runtime-only pilots may measure cost but may not expose preparation coordinates, outcome counts, endpoints, or control results.
- Lineage predecessor: none. This does not create a controlling verdict.
- Reproducibility boundary: This is an unfrozen mutable design, not a preregistration or an implemented simulator instrument. The row pins no producer, export, custody repository, seed, freeze artifact, run, raw capture, verdict receipt, source preparation, public outcome, operational readback, provenance, or custody. OL-C5 remains partial; PR-03, the public-readback remainder of PR-64, and PR-65 remain open, while PR-04 remains a declared consumed effect. Because no endpoint in this design supplies the operational derivation required to remove PR-03, the registered consequence contract makes REPLICATED support_only and FAILED block_attainment; neither verdict alone attains or owes the whole ledger row. OL-C5 has no controlling instrument, and INS-03 neither supersedes INS-01 or INS-02 nor authorizes a simulator run. Any freeze or execution requires a separate immutable preregistration and explicit authorization.
- Controls:
  - A record-diagonal source control recomputes every Born weight exactly from the exported preparation and effects, requires the phase weight to equal one half, and requires the frozen count interval to cover the recomputed weight.
  - A frozen shuffled-context control must destroy both the phase-equivalence endpoint and any apparent additivity violation; failure returns INCONCLUSIVE.
  - The declared-table comparison runs only after endpoint values are recorded, carries no promotion authority, and returns INCONCLUSIVE with a defect report on disagreement.
  - A classical reveal-only mock passes through the same observable path and must be rejected by the frozen interference check; failure to reject invalidates the estimator.
- Freeze artifacts: none (unfrozen row).
- Frozen (UTC): none.
- Verdict receipts: none (no run verdict).

Totals: 3 instruments. Status: 2 SPECIFIED, 1 FAILED.
