# Core dependencies for the mandatory scientific receipt suite (pytest.ini: testpaths = code).
# Pinned and verified from a clean clone on CPython 3.12.
#
# mpmath and sympy are imported across code/ and require explicit declarations
# for clean-clone collection (see the reproduction guide).
#
# Optional lanes keep their own extras and stay opt-in:
#   code/ibm_quantum_cloud/requirements-ibm.txt   IBM / Qiskit hardware lane (OPH_RUN_IBM=1)
#   code/particles/requirements-quantum.txt        qiskit ancillary experiments
numpy==2.5.1
scipy==1.18.0
mpmath==1.4.1
sympy==1.14.0
PyYAML==6.0.3
jsonschema==4.26.0
cryptography==46.0.5
pytest==9.1.1
# Frozen-prediction custody (evidence/custody/): `ots info` parses the detached
# OpenTimestamps proofs locally, without network access. The client cannot
# start on Windows (python-bitcoinlib finds no OpenSSL there), so it is skipped.
opentimestamps-client==0.7.2; sys_platform != "win32"
opentimestamps==0.4.5; sys_platform != "win32"
python-bitcoinlib==0.12.2; sys_platform != "win32"
