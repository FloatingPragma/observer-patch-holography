# Finite A5 capacity control

The exact A5 packet is a finite software evaluator control and a no-go witness
for raw-carrier promotion. It is not physical or cosmic N-closure.

The source-derived packet contains 60 reachable compatible public frame
records and all 60 reversible left-action continuations. On

```text
H_k = l2(A5) tensor C^k
```

the compound confusability graph is empty and

```text
M_0 = 60
D_raw = 60 k
Delta_raw = D_raw - M_0 = 60(k - 1)
```

Raw equality occurs only at `k=1`. The multiplicity `k` is invisible to every
public read. Raw dimension is therefore implementation-dependent, the
intrinsic physical capacity coordinate is undefined, and the packet emits no
physical `D_star` or `N_star`. Its equality seam is idempotent and supplies no
extensive horizon-capacity law.

The clean-room verifier replays the serialized packet semantics without
importing the constructor. It does not independently derive the packet from a
physical source. The frozen status is:

```text
FINITE_PACKET_EVALUATOR_CONTROL_RECEIPT = true
RAW_CARRIER_MULTIPLICITY_MINIMUM_RECEIPT = true
PHYSICAL_N_CLOSURE_RECEIPT = false
status = BLOCKED_EXTERNAL_PHYSICAL_DERIVATION
```

The compact integration ledger is
[`runtime/a5_finite_control_status.json`](runtime/a5_finite_control_status.json).
The full constructor and hostile-mutation suite remain in the workspace
archive until a capacity-generating physical functor supplies the missing
source family and non-idempotent selector law.
