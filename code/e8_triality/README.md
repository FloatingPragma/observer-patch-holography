# E8/Spin(8) Triality Alt(9) Certificate

Canonical certificate id:
`E8_SPIN8_TRIALITY_ALT9_DOUBLE_COVER_CERTIFICATE`

This directory is the public receipt home for the finite exceptional-symmetry
sidecar integrated into the OPH papers. The mathematical claim is an exact
matrix construction of an `Alt(9)` subgroup in `W(A8)`, its nonsplit
`2.Alt(9)` lift in `Spin(8)`, and a positive half-spin image preserving an
`E8` lattice, with mod-2 orbit fingerprints distinguishing the vector and
spin presentations before triality fusion.

Repository status: receipt skeleton. The paper text records the certificate
statement and claim boundary. Public verifier status requires committing the
raw Sage scripts, matrix data, check outputs, and hashes described in
`manifest.json`.

This certificate supports finite exceptional representation-closure
bookkeeping. It does not prove OPH, select the Standard Model quotient, close
the heterotic critical-edge gate, or count as hardware evidence.
